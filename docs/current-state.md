# 現在の状態

## 現在の main flow
1. login する。
2. 必要なときだけ `/setup/voice` で同意と既定の voice を整える。
3. `/scripts` で固定1分台本を作る。必要なら `/scripts/new?from=<scriptId>` で複製する。
4. `/scripts/[id]/listen` でお手本ボイスを生成または再利用し、英文を見ながら聞いてまねる。
5. 納得したら `/scripts/[id]/record` で自分の声を録音する。
6. 評価して、保存済み結果を atomic に保存する。
7. `/scripts/[id]/review/[takeId]` で短い評価を見て、直す / ベストを残す。
8. `/progress` で最大5本の練習スロットから成果を選び、最新結果 / ベスト結果 / 保存済み録音を見る。

## UI/UX Phase 1 の画面方針
- ユーザー向け main loop は `作る -> 聞く -> 録る -> 直す` の4語を前面に出す。
- `/scripts` は「今日の1分」を主役にし、最大5個の練習課題だけを見せる。
- voice setup / provider / cache / quota / diagnostics は主導線ではなく、必要時の設定・管理や詳細に下げる。
- review はまず score、直すところ 1〜3個、短い改善コメント、次に戻る導線だけを見せる。
- 保存済み script には日本語訳の保存先がないため、既存 DB / API contract を変えずに表示できる日本語訳は AI draft preview 内に限る。

## UI/UX Phase 1.5 の画面方針
- `/scripts` は `練習課題 X / 5` と主 CTA を先に出し、一覧 card では title / 最初の1行 / 最新スコア / 次にやることだけを見せる。
- `/scripts/new` はテンプレ全文や作り方ガイドを初期表示せず、選んだテンプレだけ details で本文を開く。
- `/record` は `マイクで録音する` を primary にし、音声ファイル選択は details に下げる。
- `/progress` は最新結果 / ベスト結果 / 次にやることを先に見せ、script ごとの履歴と詳細分析は `過去の記録を見る` に隠す。
- Export は既存の owned take audio route からベスト録音 download と結果コピーだけを UI で出す。Brush-up 本実装は data model / provider / cache 方針が必要なため準備中表示に留める。

## UI/UX Phase 1.6 の画面方針
- ユーザー向け main loop は `作る -> 聞いてまねる -> 録音して評価 -> 直す / ベストを残す -> 必要ならブラッシュアップ` として見せる。
- `/scripts` は「自分の練習場」として、練習課題数とカードを大きく見せ、その他の操作は控えめにする。
- `/listen` は単なる再生画面ではなく、`お手本を聞く -> 英文を読む -> 声に出してまねる -> 納得したら録音` の練習場にする。
- ユーザー向けの主語は `お手本` / `お手本ボイス` に寄せ、`見本音声`、`補助`、英語の route label は主導線に出さない。
- テンプレートは実引用ではなく app original の quote-inspired 練習文にする。

## UI/UX Phase 2 の画面方針
- Home / `/scripts` は、5本までの練習ストックを選ぶ毎日の練習場として見せる。
- `/progress` は分析より先に、5本までの練習スロット、最新結果、ベスト結果、保存済み録音を見せる。
- `/scripts/new` は最初に `テンプレートから選ぶ / 自分で書く / AIに作ってもらう` の3択だけを出し、フォームや詳細は選択後に出す。
- `/listen` は保存済みのお手本ボイスがある場合は再生を先に出し、作り直しは小さく表示する。声の雰囲気変更は常設 UI から外す。
- `/record` はマイク録音を主導線にし、ファイル利用、評価前提、開発用入力は詳細へ下げる。

## UI/UX improvement cadence
- UI/UX の small diff は 3〜5 個程度のまとまりで進め、production 人間確認は UI Batch ごとに行う。
- copy / heading / color / card hierarchy / spacing / small visual motifs / CTA wording / empty state / screen tone は、単発で確認を求めすぎず、Codex が checks を通しながら次の small diff へ進めてよい。
- 音声再生、録音、upload、評価、login / auth、storage、iPhone Safari 固有挙動、画面が壊れる可能性がある修正は batch にまとめず、1 件ごとに人間確認する。
- 推奨 batch は A: Home / Practice / Record、B: Listen / Review、C: Progress / Navigation / Return loops。
- batch の人間確認では「声を育てる、1分スタジオ」っぽくなったか、やりすぎていないか、スマホで読みやすいか、今日も1分だけやろうと思えるか、どの画面が一番違和感あるかを見る。

## すでに通っていること
- 主要 route handler は薄く、service に委譲している。
- `/api/evaluate` は audio-first で、client の `scriptText` を信用しない。
- review 保存は `persist_review_bundle` 経由で atomic にまとまる。
- listen は `未生成 / 保存済み音声 / 新規生成 / キャッシュ再利用` を分けて表示する。
- listen の常設 UI では voice style preset を出さず、既定の練習用 preset でお手本ボイスを生成する。provider mapping 境界と cache identity は既存のまま維持する。
- listen のお手本ボイスと review / progress の保存済み録音は、client-side の再生速度だけを `0.75x / 0.85x / 1.0x / 1.15x` で切り替えられる。listen では 3秒 / 5秒の巻き戻し・早送りもできる。provider 再生成や `script_audios` cache 変更は伴わない。
- `/scripts/new` では、手動フォームの話しやすさ診断は初期表示から外し、AI下書きの詳細確認や保存前チェックだけ details 側で見る。
- `/scripts/new` には Script Studio Phase S2 の UI mock があり、`ScriptBrief -> mock ScriptDraft -> quality report -> freeze readiness` の流れを local helper だけで確認できる。本番AI生成、保存、freeze、音声生成にはまだ接続していない。
- Script Studio Phase S2.5 として、generation prompt contract / output validation / freeze preflight を `lib/script-studio/generation.ts` に固定した。将来 AI が返す metrics / chunks / focusWords はそのまま信用せず、`englishScript` から quality report を再計算する。
- Script Studio Phase S2.6 として、`lib/script-studio/generator.ts` に generation provider / pipeline boundary と deterministic mock provider を追加した。UI は provider raw output ではなく pipeline result の accepted draft / rejected candidate を見る。
- Script Studio Phase S3a の repo-side adapter として、`lib/script-studio/openai.ts` に OpenAI Responses API adapter を追加した。client は直接 import せず、server-side route 境界の内側でだけ optional provider として扱う。DB / freeze 保存には接続していない。
- Script Studio Phase S3b として、`POST /api/script-studio/generate` と `services/script-studio` の server boundary を追加した。route は auth / schema validation / provider selection / pipeline execution / safe response shaping だけを担当し、raw OpenAI output や secret は返さない。既定 provider は `SCRIPT_GENERATION_PROVIDER=mock`。
- Script Studio Phase S3c として、`/scripts/new` の `ScriptStudioMockPanel` から `POST /api/script-studio/generate` を呼ぶ draft preview を追加した。UI は route の safe response だけを表示し、provider selection は server env の責務に留める。script 保存 / freeze / quota / voice generation にはまだ接続していない。
- Script Studio Phase S3d として、accepted draft の `title / englishScript / target seconds` だけを既存の手動 script 作成フォームへコピーできるようにした。コピー後はフォーム内で編集してから既存保存 flow を使う。freeze / quota / voice generation にはまだ接続していない。
- Script Studio Phase S3e として、freeze / quota / cost control の設計を `docs/script-studio-freeze-quota-plan.md` に固定した。generated draft は一時 preview、copied draft は editable form state、保存後の `scripts` row が当面の canonical という前提を明文化している。
- Script Studio Phase S4a として、`/scripts/new` の手動フォームに表示用の freeze preflight preview を追加した。フォーム内の現在台本を local helper で判定するだけで、保存 / freeze / 音声生成 / quota 消費は行わない。
- Script Studio Phase S4b として、`/scripts/[id]/listen` に保存済み script の read-only freeze candidate check を追加した。saved script を canonical として表示用に判定するだけで、listen の見本音声生成 / cache / replay は止めない。
- Script Studio Phase S4c として、MVP では saved script only で進め、`script_freezes` table は今すぐ作らない判断を docs に固定した。freeze は当面、音声生成前の read-only preflight / UX boundary として扱い、canonical data にはしない。
- Script Studio Phase S4d として、見本音声生成直前の preflight design を `docs/script-studio-voice-generation-preflight-plan.md` に固定した。saved script を server 側で読み直す、cache reuse と regeneration を分ける、voice setup / provider / storage / quota の確認観点を整理しただけで、listen flow / API / DB / UI / gating は変更していない。
- Script Studio Phase S4e として、`/scripts/[id]/listen` に音声生成前の UX copy / preflight only 表示を追加した。saved script から見本音声を作ること、cache reuse と regeneration の違い、voice setup / provider readiness が必要なことを説明するだけで、listen flow / API / cache key / gating は変更していない。
- Script Studio Phase S4f として、quota event design を `docs/script-studio-quota-event-plan.md` に固定した。text generation / voice generation / voice clone の quota 境界、消費対象・非消費対象、cache reuse / regeneration、failed / partial event、将来の `quota_events` model 候補を整理しただけで、DB / API / UI / billing / enforcement は変更していない。
- Script Studio Phase S4g として、`/scripts/[id]/listen` の preflight notice に provider readiness / default voice / saved audio / cache behavior の状態表示を追加した。cache hit は quota 消費ではなく、regeneration は将来 quota 対象になり得るが未実装であることを説明するだけで、listen flow / API / cache key / gating は変更していない。
- Script Studio Phase S4h として、`/scripts/new` と `/scripts/[id]/listen` に quota preflight copy only を追加した。draft generation / regeneration、cache miss の見本音声生成、provider synthesize は将来 quota 対象になり得る一方、copy / manual edit / saved script creation / cache hit / protected replay は quota 消費ではないと説明するだけで、DB / API / route / cache key / enforcement は変更していない。
- Script Studio Phase S4i として、`/scripts/new` と `/scripts/[id]/listen` の quota / freeze / voice preflight copy density を見直し、compact な quota notice に整理した。境界の意味は残し、DB / API / route / cache key / enforcement は変更していない。
- Script Studio Phase S5a として、将来の `quota_events` schema design を docs に固定した。event type / status / subject / target resource / idempotency / dedupe / retention / privacy / RLS / ownership を整理しただけで、DB schema / migration / API / write path / enforcement / billing は変更していない。
- Script Studio Phase S5b として、text generation quota event write path design を docs に固定した。将来 `POST /api/script-studio/generate` で event を書く場合は server-side service 境界で orchestration し、route / pipeline / provider adapter の責務を崩さない方針を整理しただけで、DB schema / migration / API contract / write path / enforcement / billing は変更していない。
- Script Studio Phase S5c として、voice generation quota event write path design を docs に固定した。将来 `/api/speak-script` で event を書く場合は `speakScript` service 境界で orchestration し、cache hit / cache miss / provider synthesize / replay staging / protected replay failure を分ける方針を整理しただけで、DB schema / migration / API contract / route / cache key / write path / enforcement / billing は変更していない。
- Script Studio Phase S5d として、quota event implementation readiness design を docs に固定した。最初の実装対象は text generation、voice generation はその後、write failure は当面 non-blocking、`rejected` は status ではなく `failed + failure_stage` で表す方針を整理しただけで、DB schema / migration / API contract / write service / enforcement / billing は変更していない。
- Script Studio Phase S5e として、text generation quota event implementation plan を docs に固定した。将来の `quota_events` migration 候補、RLS、write service interface、`generateScriptStudioDrafts` からの呼び出し位置、`billing_status`、write failure logging、privacy-safe metadata を具体化しただけで、DB schema / migration / API contract / write service / enforcement / billing は変更していない。
- Script Studio Phase S5f として、`script_generation_attempt` だけを対象に `quota_events` migration / DB types / non-blocking write service / `generateScriptStudioDrafts` 接続を追加した。API response shape は変えず、write failure は generation 本体を止めない。voice generation / voice clone / enforcement / billing / usage dashboard は未実装。
- S5f-db-smoke-plan として、dev DB に `0009_phase_s5_quota_events.sql` を適用した後の table / constraints / partial unique / RLS / mock provider smoke / privacy 確認 checklist を `docs/script-studio-quota-event-plan.md` に追加した。secret 操作、手動ブラウザ確認、本番運用は前提にしていない。
- Script Studio Phase S5g として、voice generation quota event の初回実装計画を docs に固定した。初回対象は `script_audio_generation_attempt` のみで、cache hit / cache miss、status / failure_stage、dedupe / fingerprint、privacy、non-blocking write failure を整理しただけで、DB migration / write service / API contract / enforcement / billing / UI は変更していない。
- Script Studio Phase S5h として、voice generation quota event 実装前の `quota_events` schema extension plan を docs に固定した。`0009` は変更せず、将来 `script_audio_generation_attempt / voice_generation_quota / cache_hit / partial / saved_script / script_audio` などを後続 migration で許可する案を整理しただけで、DB migration file / write service / speakScript 接続は追加していない。
- Script Studio Phase S5i として、voice generation quota event 用の schema extension を `0010_phase_s5_voice_quota_events.sql` で追加した。DB types と quota write service は `script_audio_generation_attempt` / voice metadata を受けられるが、dev DB migration apply、`/api/speak-script` / `speakScript` 接続、実記録、enforcement / billing / UI はまだ追加していない。
- Script Studio Phase S5j として、`speakScript` service 境界に `script_audio_generation_attempt` の non-blocking quota event write を接続した。cache hit は `cache_hit / non_billable`、cache miss から provider synthesize に進む場合は `attempted -> succeeded/failed/partial` を記録するが、API response shape / cache key / provider behavior は変えない。dev DB に `0009/0010` が未適用の場合は quota write warning に留まり、見本音声生成本体は止めない。
- S5k-plan として、dev DB に `0009/0010` を適用した後の text generation / voice generation 統合 smoke checklist と SQL 確認 query を `docs/script-studio-quota-event-plan.md` に固定した。dev DB migration apply、secret 操作、手動ブラウザ確認はまだ実施しない。
- S5 close check として、dev DB に `0009/0010` 適用後、mock の text generation route と mock の voice generation / cache hit route は API response shape を変えずに通ることを確認した。`quota_events` には `script_generation_attempt=not_billable`、`script_audio_generation_attempt=succeeded/cache_hit` が記録され、forbidden metadata key は 0 件だった。RLS spot check では authenticated user の own select は通り、client insert は拒否された。
- Audio Library Phase S6a として、生成した見本音声と自分のベスト録音を script ごとに最大 5 件残す設計を `docs/audio-library-plan.md` に固定した。S6b として migration / RLS / service interface / API boundary plan を `docs/audio-library-migration-plan.md` に固定した。S6c では `0011_phase_s6_audio_library.sql`、DB types、`services/audio-library` の service skeleton を追加した。S6d では saved model audio、S6e では saved best take の API boundary を追加した。S6f で listen の見本音声保存 UI、S6g で review のベスト録音保存 UI、S6h で `/progress` の script-scoped Audio Library summary を追加した。S6i で listen 保存、review ベスト保存、progress 表示、保存解除後の underlying audio/take preserved を smoke 済み。quota enforcement と global library UI はまだ追加していない。
- Speed / voice style Phase S7a として、`docs/speed-voice-style-plan.md` に playbackRate、生成時 target speed / WPM / pause density、voice style preset、Script Studio / listen / Audio Library の接続方針を design-only で固定した。DB migration / API contract / UI 実装 / provider 本接続はまだ追加していない。
- Speed / voice style Phase S7b として、`lib/voice-style.ts` に拡張 voice style preset id / speed intent / pause density / preset definition を追加し、`providers/voice/style-mapper.ts` に provider-specific mapper 境界を追加した。既存 UI / API の accepted preset は `natural / expressive / clear / slow` のまま維持し、DB / API contract / UI は変更していない。
- Speed / voice style Phase S7c として、既存4 preset の user-facing copy を「生成 style」として整理し、`slow` は将来 `slow-practice` へ寄せる compatibility alias としてコード上に残した。public schema、request value、cache key に入る値は変更していない。
- Speed / voice style Phase S7d として、`/scripts/[id]/listen` の copy を小さく整理し、生成 style は見本音声そのものの話し方、playbackRate はこの画面で聞く速さだけ、という違いを読みやすくした。API / DB / cache key / public schema は変更していない。
- Speed / voice style Phase S7f として、`/scripts/[id]/listen` の表示密度を下げ、主操作を先に見せ、台本 check / voice preflight / quota / cache の詳しい説明は details に整理した。追加の small diff で、見本音声の生成 / 更新 / 再生準備と Audio Library の保存 / 保存解除の pending feedback を明示した。API / DB / cache key / public schema は変更していない。
- Speed / voice style Phase S7g として、saved model audio の safe metadata snapshot を少し整理した。新規保存分では既存 cache key から推定できる範囲で generation style / target speed / target WPM / pause density を保存し、listen / progress では provider / voice / style を短く表示する。旧データや推定不可の保存済み音声では `style: 旧データ / 詳細なし` と表示する。playbackRate は保存済み音声 identity に含めず、API / DB / cache key / public schema は変更していない。
- Speed / voice style Phase S7h として、provider-specific mapping 境界を mock 前提で確認した。`voiceStylePreset` は service / cache / quota metadata / provider `synthesize` input へ渡るが、mock provider は音質へ反映せず境界確認だけに使う。実 provider の変換は `providers/voice/style-mapper.ts` に閉じ、playbackRate は provider mapping に混ぜない。
- Speed / voice style Phase S7i として、`npm run voice:style-smoke` を追加した。provider API を呼ばず、`voiceStylePreset` が provider input / cache / Audio Library metadata の境界を通ること、`playbackRate` が provider / cache / saved audio identity に混ざらないこと、OpenAI が ad hoc style prompt text を受け取らないことを静的に確認できる。
- S8a として、実 provider live smoke 前の前提条件、ElevenLabs / OpenAI の先行候補比較、失敗時の `VOICE_PROVIDER=mock` 切り戻しを `docs/voice-provider-smoke-plan.md` に固定した。その後のユーザー実ブラウザ確認で、Native Minute から ElevenLabs clone voice を作成し、その clone voice で `/scripts/[id]/listen` の script を読み上げるところまでは成功済みとして扱う。DB / API / cache key / preset 変更はしていない。
- S8c として、ElevenLabs residual smoke を dev DB / code path から spot check した。同一条件の cached `script_audios` row、`natural / expressive / slow` の別 cache key / 別 row、`script_audio_generation_attempt=succeeded` と privacy-safe metadata、Audio Library の旧データ fallback は確認済み。
- S8d として、ElevenLabs 実ブラウザ操作後に同一条件再生成の `cache_hit / non_billable` quota row、`script_audios` が重複しないこと、fresh Audio Library save row の `voice_style_preset / voice_style_label / target speed` metadata、raw script / audio bytes / signed URL / raw provider payload / playbackRate が metadata に入っていないことを確認した。
- S8e として、ElevenLabs failure branch / cleanup policy を `docs/voice-provider-smoke-plan.md` に固定した。sample reject、verification required、rate limit、billing / entitlement、invalid or deleted provider voice、synthesize failure、storage staging failure、protected replay failure を分け、provider voice cleanup は MVP では自動化しない方針にした。server log は request id / failure point / code/type / short message に留め、raw provider response body は出さない。
- S8f として、ElevenLabs failure branch の user-facing copy を small diff で整理した。setup/voice では sample / verification / account or plan / rate limit を分け、listen では deleted provider voice / account or rate limit / storage staging を分けて recovery を出す。provider cleanup API、DB / API / cache key / preset は変更していない。
- S8g として、ElevenLabs `clear` style の live row と quota event を確認し、`natural / expressive / clear / slow` が同じ script / voice で別 cache key / 別 `script_audios` row になることを確認した。metadata privacy spot check でも raw script / raw provider payload / signed URL / audio bytes / playbackRate は保存されていない。実音声の style 差はあるが微量で、style 名どおりかは判断しづらい。
- S9a として、`/scripts/new` を Script Studio entry として整理し、`テンプレから選ぶ / フリーライティング・既存原稿貼り付け / AI自動生成 / このアプリ向きの文章ガイド` の4入口を追加した。テンプレは安全な自作英文だけを少数持ち、既存フォームへコピーして編集する。DB schema / API contract / script 保存形式は変更していない。
- S10b/S10c/S10e として、`/scripts/new` の4入口を browser smoke し、テンプレコピー後にフォームへ移動しやすくした。手動フォーム内は `入力 -> 目標秒数 -> 話しやすさ -> 保存前チェック -> quota 補足 -> 保存` の順に寄せ、保存後の `/scripts/[id]/listen` では作成直後だけ見本音声・record・複製修正への handoff を短く出す。DB schema / API contract / script 保存 flow は変更していない。
- S10f として、AI draft 入口を監査し、既定は mock provider の確認用 preview であり、本番AI生成の品質確認ではないことを UI copy で明確にした。OpenAI adapter は server-side route 境界の内側にあるが、client から provider や secret は触らず、保存前は必ず手動フォームで編集する前提を維持する。
- S10g として、AI draft の seed 例と品質期待値を整理した。UI では `seed 例を見る` と `draft の見方` を details に置き、仕事で小さく成功した話、最近少し困ったこと、自分の好きなもの、挑戦したいこと、感情がある短い出来事から始められるようにした。良い draft は 45〜75秒、1テーマ、自分が言いそう、意味の塊で練習しやすいことを目安にする。
- S10h として、AI mock preview の browser smoke を実施した。`seed 例を見る -> seed 入力 -> draft 作成 -> draft の見方 -> フォームへコピー -> 編集 -> 保存 -> /scripts/[id]/listen?created=1` の流れを確認し、OpenAI live smoke、DB schema、API contract、script 保存 flow は変更していない。
- S10i として、Script Studio OpenAI live smoke readiness を確認した。既定 provider は `mock`、`openai` は `SCRIPT_GENERATION_PROVIDER=openai` と `OPENAI_API_KEY` があるときだけ server route 内で使う。client は provider adapter / secret / raw response に触れず、OpenAI provider request id は quota metadata 用に内部保持し、UI issues には出さない。live API 実行はまだ行っていない。
- S10j として、`SCRIPT_GENERATION_PROVIDER=openai` を process env で一時指定し、OpenAI live generation smoke を実施した。日本語 seed から accepted draft が出て、quality report / freeze preflight / form copy / 編集保存 / `/scripts/[id]/listen?created=1` まで通った。`quota_events` は `script_generation_attempt=succeeded / billable_candidate` を記録し、metadata に raw seed / generated draft text / raw provider response / secret / playbackRate は入っていない。DB schema / API response shape / script 保存 flow / cache key は変更していない。
- S10k として、OpenAI draft quality を 5 種類の日本語 seed で小さく監査した。初回は accepted draft が全件出たが 60秒目標に対して短めに寄ったため、prompt pack に target word range と「短い seed でも具体的な理由・例を足す」指示を追加した。再確認では全 seed が accepted、78〜93 words、blocking reason なしになり、短すぎる傾向は改善した。いくつかは long chunk warning が残るので、完成品ではなくフォームで編集する候補として扱う。quota metadata は引き続き raw seed / generated draft text / raw provider response / secret / playbackRate を保存しない。
- S10l として、OpenAI draft の UI browser smoke を行った。`/scripts/new` の AI 入口で seed 例を選び、real OpenAI draft を作成し、quality report / freeze readiness を見てからフォームへコピー、軽く編集、保存、`/scripts/[id]/listen?created=1` まで通った。UI は raw seed を入力欄以外で再掲せず、long chunk warning はエラーではなく編集目安として案内する。DB schema / API response shape / script 保存 flow / cache key は変更していない。
- S10m として、OpenAI draft の long chunk / editability を小さく改善した。favorite / future challenge / emotional short event 系の seed で live 生成を見直し、prompt pack に `practiceShape / chunkShape / sentenceBoundary` を追加して、75〜100 words 目安を維持しながら comma / period の自然な息継ぎ境界へ寄せた。再確認では 3 seed が 79〜89 words、全件 accepted、long chunk 0、blocking reason なし、freeze preflight 可になった。UI copy は long chunk warning を comma / period で区切る編集目安として案内し、DB schema / API response shape / script 保存 flow / cache key は変更していない。
- S10n として、Script Studio の integrated entry smoke を実施した。テンプレ、フリーライティング、AI draft の 3入口それぞれで、入力またはコピー、軽い編集、保存、`/scripts/[id]/listen?created=1` handoff まで通ることを Playwright で確認した。説明量、保存前チェック、quota 補足、AI warning copy は既存のままで大きな違和感はなく、DB schema / API response shape / script 保存 flow / cache key は変更していない。
- S11a として、Script Studio 保存後の main loop handoff を Playwright で確認した。テンプレ入口から script を作成し、`/scripts/[id]/listen?created=1` の作成直後案内、mock 見本音声生成、record upload + mock transcript fallback、review、progress の該当 script card / review link まで通った。DB schema / API response shape / script 保存 flow / cache key は変更していない。
- S11b として、Script Studio 由来 script の複数回練習 continuity を Playwright で確認した。同じ script に 2 件の take を保存し、1回目と2回目それぞれで review / progress に反映され、progress の `最新結果 / ベスト結果 / 改善の流れ / 最新とベストの差` と review link / record / listen 導線が崩れないことを確認した。DB schema / API response shape / script 保存 flow / cache key は変更していない。
- S12a として、実 provider 本接続前の readiness を `docs/provider-readiness-plan.md` に固定した。`/api/evaluate` audio-first、server-owned script re-fetch、atomic review persistence、canonical review/progress source、storage ownership、provider fallback を壊さない contract と、OpenAI transcription / Azure pronunciation evaluator / voice provider / Script Studio OpenAI generation の env、storage、manual smoke、failure recovery を整理した。実 provider 追加実装、DB schema、API response shape は変更していない。
- S12b として、Azure pronunciation evaluator の hardening / manual smoke を実施した。Azure adapter は live API 呼び出し実装済みで、stub ではない。Azure cancellation / start failure の user-facing error から raw Azure detail を外し、`TRANSCRIPTION_PROVIDER=mock` + 補助 transcript + `PRONUNCIATION_PROVIDER=azure` で `record -> evaluate -> review -> progress` が通ることを確認した。`TRANSCRIPTION_PROVIDER=openai` との組み合わせでは fixture audio の transcript が空で、Azure に進む前に safe error で止まった。DB schema / API response shape / evaluate contract / review persistence / cache key は変更していない。
- S12c として、OpenAI transcription live smoke を 30〜60秒程度の local-only 英語 WAV で再確認した。`TRANSCRIPTION_PROVIDER=openai` + `PRONUNCIATION_PROVIDER=mock` で空でない transcript が review / progress へ反映され、続いて `TRANSCRIPTION_PROVIDER=openai` + `PRONUNCIATION_PROVIDER=azure` でも review / progress まで通った。OpenAI provider error は raw body を user-facing response に混ぜないよう hardening し、empty transcript は録音長・無音・明瞭さを確認する recovery に寄せた。DB schema / API response shape / evaluate contract / review persistence / cache key は変更していない。
- S12d として、browser microphone から `OpenAI transcription + Azure pronunciation evaluator` へ進む人間録音 smoke checklist を `docs/provider-readiness-plan.md` に追加した。S12d 時点では Codex 単独で人間のマイク録音を実施できないため、manual smoke は未実施だった。実装上は browser 録音が webm になっても、Azure 選択時は upload 前に client-side で wav/PCM へ正規化してから保存する。empty transcript の recovery は、同じ録音の再試行ではなく、30〜60秒程度の明瞭な英語で録り直す案内に寄せた。DB schema / API response shape / evaluate contract / review persistence / cache key は変更していない。
- S12e として、ユーザー実ブラウザの人間マイク録音で `TRANSCRIPTION_PROVIDER=openai` + `PRONUNCIATION_PROVIDER=azure` の real-provider evaluation loop が通過した。review へ遷移し、transcript は空ではなく、score grid / weak words が表示され、progress に反映された。録音時間の詳細は未記録だが、S12e smoke は passed として扱う。
- S12f として、S12b〜S12e の結果を `docs/provider-readiness-plan.md` に同期し、real-provider evaluation loop を一区切りにした。DB schema / API response shape / evaluate contract / review persistence / canonical source / cache key は変更していない。
- S13a として、本番前 manual QA checklist と targeted failure smoke plan を `docs/provider-readiness-plan.md` に追加した。happy path は script 作成、listen、30〜60秒録音、OpenAI transcription + Azure evaluation、review、progress、2回目練習、mobile / desktop、refresh / back navigation を見る。S13b では empty transcript / too short、OpenAI env missing、Azure env missing を優先する。DB schema / API response shape / evaluate contract / review persistence / canonical source / cache key は変更していない。
- S13b として、targeted failure smoke 3 件を確認した。短すぎる local-only PCM WAV は OpenAI transcription の safe error になり、30〜60秒程度の明瞭な英語で録り直す recovery に進む。isolated env で OpenAI transcription env missing と Azure env missing を再現し、どちらも provider 設定不足として切り分けた。partial review / progress は保存しない前提を確認し、secret / raw provider response / raw audio は表示・保存していない。
- S13c として、本番前 manual QA 実走パックを `docs/provider-readiness-plan.md` に追加した。desktop / mobile の happy path、listen / record / review / progress の pass criteria、mobile / Safari 注意点、QA 結果記録テンプレート、Codex 自動確認と人間確認の分担を整理した。実装変更、DB schema、API response shape、evaluate contract、review persistence、canonical source、cache key は変更していない。
- UX-R1 として、`/` を practice-first Home に寄せた。ログイン後の first view は「今日の1分」を始める CTA、前回の続き、新しい練習追加、最新結果の短い日本語要約、progress への小さい導線を中心にし、voice 設定は不足時だけ補助表示する。top nav は `Home / Practice / Progress / Settings` に寄せ、`/scripts` は Practice library として別 script 選択・複製・新規追加の場にした。DB schema、API response shape、provider contract は変更していない。
- UX-R2 として、`/scripts/[id]/review/[takeId]` の first view を summary-first に圧縮した。最初は総合スコア、今日の直すポイント 1〜3 個、一言アドバイス、もう一度録音する CTA を出し、transcript 全文、score grid、weak words 詳細、比較、technical detail は「詳細を見る」へ下げる。review persistence、canonical source、`/api/evaluate` contract、DB schema は変更していない。
- UX-R3 として、`/scripts`、`/scripts/[id]/listen`、`/scripts/[id]/record` の first view を practice-first に寄せた。Practice library は「今日の練習を選ぶ」主 CTA を先に出し、listen は「見本を短く聞いて record へ」、record は「録音して評価へ」を主語にした。台本全文、practice chunks、cache / freeze / quota / provider 前提、recovery の詳しい説明は details 側へ下げた。listen / record / upload / evaluate の flow、DB schema、API contract は変更していない。
- UX-R4 として、voice setup と Script Studio を普段使いの主導線から一段奥へ下げた。`/setup/voice` は「見本音声が必要なときの設定」として見せ、provider readiness / diagnostics / manual smoke / entitlement recovery は details 側へ寄せた。`/scripts/new` は「新しい練習を追加する」画面として、テンプレ / 自由入力 / AI draft を練習追加の手段に寄せた。v1 mainline の voice provider は ElevenLabs、OpenAI は transcription / script generation / coaching、Azure は pronunciation evaluator という copy にそろえ、DB schema / API contract / provider contract は変更していない。
- UX-R5 として、store screenshot readiness 用の visual polish を小さく入れた。Home / Practice library / Add practice / setup voice / listen / record / review / progress の first view は、practice-first の情報設計を保ったまま、余白、hero surface、主 CTA、mobile 幅の button stacking、summary-first review card を整えた。DB schema / API contract / provider contract / auth / storage は変更していない。
- RR-2a として、request-based account deletion の受け皿になる `account_deletion_requests` migration を追加した。削除実行順は provider cleanup -> storage cleanup -> DB cleanup -> Supabase Auth user deletion を前提にし、Auth user deletion は最後、active request は user ごとに 1 件。RR-2a-preflight では direct client insert を閉じ、RLS は own read のみにした。RR-2b では `/settings`、`GET /api/account/deletion-status`、`POST /api/account/deletion-request`、`POST /api/account/deletion-confirm` を追加し、request 作成・確認・状態表示だけを server-side 経由で行う。RR-2c では `GET /api/account/deletion-inventory` と safe inventory dry-run を追加し、DB row counts / storage object counts / ElevenLabs voice candidate count だけを server-side で集計する。RR-2d では `GET /api/account/deletion-job-dry-run` と stage guard summary を追加し、provider cleanup -> storage cleanup -> DB cleanup -> Supabase Auth deletion -> completion の順序だけを dry-run で確認する。RR-2e では `GET /api/account/deletion-provider-dry-run` と ElevenLabs provider cleanup candidate summary を追加し、provider voice id を返さず required / blocked / not_needed の件数だけを見る。RR-2f では `GET /api/account/deletion-storage-dry-run` と bucket 別 storage cleanup candidate summary を追加し、storage path / object key / signed URL を返さず DB-known count、listed count、orphan/missing count だけを見る。RR-2g では `GET /api/account/deletion-database-dry-run` と table 別 DB cleanup candidate summary を追加し、cascade dependent / explicit delete / delete-last / retain-anonymize の分類と件数だけを見る。RR-2h では `GET /api/account/deletion-auth-dry-run` と Supabase Auth deletion preflight を追加し、service role 必須、DB cleanup 待ち、Auth user presence、anonymized tracking 前提を safe summary だけで見る。row id、script 本文、transcript、raw metadata、auth raw payload、session detail は返さない。deletion job runner、storage cleanup、ElevenLabs delete、DB row delete/update/anonymize、Supabase Auth deletion はまだ追加していない。
- Gate 0 auth smoke は human browser で完了扱い。Settings logout、magic link login、callback -> `/scripts`、refresh session、re-login が通り、`/login` 404 / `_next/static` 404 は再発していない。rate limit 時は raw detail なしの日本語 message を表示する。
- Gate 0 後の `/setup/voice -> listen` 再開 smoke では、`/setup/voice`、`/scripts/[id]/listen`、`/api/speak-script`、`/api/script-audio/[audioId]` が 200 で通り、ElevenLabs の見本音声は実ブラウザで再生できた。2回目の生成 / 再生も実用上は問題なかったが、今回のログだけでは cache reuse が `保存済み音声の再利用` になったかは断定しない。必要なら Gate 1 の cost guard / cache behavior spot check として DB / quota event / `script_audios` を確認する。
- Gate 1 Web production readiness audit を `docs/gate1-production-readiness-audit.md` に追加した。結論は、main loop と UX はかなり通っているが、Web production には production provider/env guard、production Supabase Storage / RLS spot check、quota/cost guard、privacy/support/account deletion actual path の判断がまだ必須。
- Gate1a として、production env / provider guard を追加した。`npm run production:preflight` は strict production (`VERCEL_ENV=production`、`NATIVE_MINUTE_ENV=production`、または `NATIVE_MINUTE_PRODUCTION_GUARD=1`) で `VOICE_PROVIDER=elevenlabs`、`TRANSCRIPTION_PROVIDER=openai`、`PRONUNCIATION_PROVIDER=azure`、`SCRIPT_GENERATION_PROVIDER=openai` を要求し、E2E/test helper env を blocked にする。runtime でも strict production では mock provider と E2E/test helper route を拒否する。local/dev の mock fallback は維持する。
- Gate1b として、production Supabase / Storage / RLS runbook を `docs/gate1b-supabase-storage-rls-runbook.md` に追加し、`npm run supabase:storage-rls:check` を追加した。checker は migration file presence、required table reachability、required buckets の存在と private 設定だけを non-destructive に確認し、secret 値、raw storage path、object key、signed URL、provider voice id は出力しない。human protected replay check では User A own script-audio / take-audio replay が 200 相当で再生でき、User B cross-user replay は 403/404 相当で再生できなかった。
- Gate1b proof 固定として、production Supabase 上の migrations / buckets / RLS / protected replay / ownership を人間が証跡化する template `docs/gate1b-production-supabase-spot-check-proof-template.md` を追加した。2026-05-08 の protected replay / cross-user ownership PASS を raw id なしで記録済み。secret、raw user id、storage path、object key、signed URL、audio id / take id、raw audio は記録せず、PASS/WARN/BLOCKED/FAIL と count / status / masked project ref だけを残す。
- Gate1c として、quota / cost guard 方針を `docs/gate1c-quota-cost-guard-decision.md` に固定した。v1 無料公開は `private_beta` または `small_cohort` から始め、DB-backed quota enforcement なしの `public_free` は production preflight で blocked にする。`NATIVE_MINUTE_DISABLE_OPENAI / NATIVE_MINUTE_DISABLE_AZURE / NATIVE_MINUTE_DISABLE_ELEVENLABS / NATIVE_MINUTE_DISABLE_STORAGE_UPLOADS` の env kill switch を追加し、OpenAI transcription / Script Studio generation、Azure evaluator、ElevenLabs voice clone / TTS、user upload routes を安全に一時停止できる。quota_events は引き続き logging only で、per-user hard quota / dashboard / billing は未実装。
- Gate1g として、provider budget / kill switch operations の runbook と proof template を `docs/gate1g-provider-budget-kill-switch-runbook.md` と `docs/gate1g-provider-budget-kill-switch-proof-template.md` に追加し、人間 dashboard proof を raw 値なしで反映した。2026-05-10 の Azure update で、Azure Speech resource / region / quota / usage visibility、Keys and Endpoint page visibility、pricing tier visibility、pay-as-you-go upgrade、Cost Management、kill switch は PASS。2026-05-11 の human browser smoke で Azure pronunciation live smoke も PASS。Supabase Storage usage / egress と support contact / deletion SLA も PASS。OpenAI dedicated project / app-side hard cap、ElevenLabs alert、legal/support draft clarity は WARN だが、release owner が small-cohort Web beta では受容した。Gate1g は `WARN`、Web beta decision は `GO WITH WARNINGS` とする。raw API key、billing amount、account id、subscription id、project id、resource id、key、endpoint、signed URL、storage path、raw provider response は記録していない。
- 人間 dashboard / billing / legal / SLA 判断が必要な項目は `docs/human-check-backlog.md` にまとめる。2026-05-10 の final batch で support contact は `bakkanakuma@gmail.com`、account deletion first response target は 3 business days、completion target は 30 days、manual cleanup owner は app owner として PASS。formal legal review は未完了なので `/privacy`、`/terms`、`/support`、`/support/account-deletion` は WARN のまま。2026-05-11 の Gate1l update で release / deploy / rollback / incident / post-deploy smoke / provider monitoring / support-deletion owner は app owner、Azure pronunciation live smoke は PASS として記録した。
- Gate1h として、Web beta deploy / rollback / post-deploy smoke runbook を `docs/gate1h-web-beta-deploy-rollback-smoke-runbook.md` に追加した。production-like deploy 前は `production:preflight`、`supabase:storage-rls:check`、provider preflight、kill switch readiness、Human Check Backlog を確認し、deploy 後は login、scripts、setup/voice、listen、record/evaluate、review、progress、protected replay、legal/support draft routes を smoke する。実デプロイ、env 変更、dashboard 操作、DB/RLS/provider contract 変更はしていない。
- Gate1i として、Web beta release candidate proof / deploy log template を `docs/gate1i-web-beta-release-candidate-proof-template.md` に追加した。誰が・いつ・どの commit/env/check 結果で `GO / GO WITH WARNINGS / BLOCKED` を判断したか、deploy 実行時の pre/post smoke と rollback 判断を raw 値なしで残す。Human Check Backlog の未解消項目は PASS にせず、実デプロイや production env 変更はしていない。
- Gate1j として、final human-check batch runbook を `docs/gate1j-final-human-check-batch-runbook.md` に追加した。Azure Speech resource / quota visibility、OpenAI project / budget、ElevenLabs alert、Supabase Storage usage / egress、support contact、account deletion SLA、legal/support draft review について、owner、確認場所、手順、PASS/WARN/BLOCKED 条件、記録先、記録禁止 raw 値を整理した。2026-05-10 に support contact / deletion SLA / Supabase Storage visibility は PASS として記録済み。実デプロイと法務承認はしていない。
- Gate1d として、privacy / support / account deletion actual path decision を `docs/gate1d-privacy-support-account-deletion-decision.md` に固定した。Web beta / small cohort は request-based deletion + support/manual cleanup 運用で進められる可能性があるが、Store submission は actual account/data deletion completion path を blocker にする。Privacy / Terms / Support / public account deletion URL は未実装で、destructive deletion job、Storage cleanup、ElevenLabs cleanup、DB cleanup、Supabase Auth deletion はまだ追加していない。
- Gate1e として、Web beta 用の public draft route `/privacy`、`/terms`、`/support`、`/support/account-deletion` を追加した。各 page は `Web beta draft` と明示し、正式公開 / Store submission / 法務レビュー前に最終確認が必要であることを示す。Settings と global footer から最小導線を置いたが、account deletion actual cleanup、Storage cleanup、ElevenLabs cleanup、DB cleanup、Supabase Auth deletion はまだ追加していない。
- Gate1f として、Web beta 前の production-like manual QA runbook を `docs/gate1f-web-beta-manual-qa-runbook.md` に追加した。clean / existing account、Home / Practice / Script Studio、listen、record、OpenAI transcription + Azure evaluation、review、progress、2回目 take、Settings、account deletion dry-run、legal/support draft、mobile browser、failure spot check の PASS / WARN / BLOCKED / FAIL を固定した。support contact、account deletion SLA、manual cleanup owner は 2026-05-10 に PASS。Gate1e draft legal/support review は WARN として残る。
- RR-3a として、Store submission 前に必要な actual account/data deletion completion path の実装計画を `docs/rr-3a-account-deletion-actual-implementation-plan.md` に固定した。provider cleanup -> Storage cleanup -> DB cleanup / anonymize -> Supabase Auth deletion -> completion の順序、failure status、retry / manual fallback、destructive 実行前 safety gate を整理した。
- RR-3b として、ElevenLabs provider cleanup actual の service / adapter 境界を destructive guard 付きで追加した。`NATIVE_MINUTE_ENABLE_ACCOUNT_DELETION_DESTRUCTIVE=1`、confirmed request、request id 一致、provider dry-run runnable、ElevenLabs cost guard off がそろわない限り ElevenLabs delete は呼ばず、status update もしない。public API / Settings 実削除ボタンは追加しておらず、Storage cleanup、DB cleanup / anonymize、Supabase Auth deletion にはまだ進んでいない。
- RR-3c として、Storage cleanup actual の service 境界を destructive guard 付きで追加した。provider cleanup が `succeeded / not_needed`、confirmed request、request id 一致、storage dry-run runnable、`NATIVE_MINUTE_ENABLE_ACCOUNT_DELETION_DESTRUCTIVE=1` がそろわない限り Supabase Storage delete は呼ばず、status update もしない。public API / Settings 実削除ボタンは追加しておらず、DB cleanup / anonymize、Supabase Auth deletion にはまだ進んでいない。
- RR-3d として、DB cleanup / anonymize actual の service 境界を destructive guard 付きで追加した。provider cleanup と Storage cleanup が `succeeded / not_needed`、confirmed request、request id 一致、DB dry-run classification 一致、`NATIVE_MINUTE_ENABLE_ACCOUNT_DELETION_DESTRUCTIVE=1` がそろわない限り DB delete / update / anonymize は呼ばず、status update もしない。public API / Settings 実削除ボタンは追加しておらず、Supabase Auth deletion にはまだ進んでいない。
- RR-3e として、Supabase Auth deletion actual の service 境界を destructive guard 付きで追加した。provider / Storage / DB cleanup がすべて `succeeded / not_needed`、confirmed request または `auth_cleanup_failed`、request id 一致、Auth dry-run runnable、service role boundary、`NATIVE_MINUTE_ENABLE_ACCOUNT_DELETION_DESTRUCTIVE=1` がそろわない限り Supabase Auth user delete は呼ばず、status update もしない。成功時は既存 schema のまま `status=completed`、`auth_cleanup_status=succeeded`、`notification_status=not_needed` として request id ベースで完了記録する境界だけを追加した。public API / Settings 実削除ボタンは追加しておらず、real Auth deletion は実行していない。
- RR-3f として、将来の disposable-account destructive audit 用 runbook / proof template を `docs/rr-3f-destructive-audit-operator-runbook.md` に追加した。provider -> Storage -> DB -> Auth -> completion の実行前 checklist、stage ごとの PASS/WARN/BLOCKED/FAIL、partial failure / manual_required、rollback 不能性、raw 値を記録しない proof 形式を固定しただけで、destructive guard は有効化せず、real provider / Storage / DB / Auth delete は実行していない。
- RR-3g として、将来の operator/admin execution surface design を `docs/rr-3g-operator-admin-execution-surface-design.md` に追加した。最小方針は public UI/API ではなく、内部 operator CLI を 1 stage ずつ dry-run default / destructive guard 必須で実行する設計。operator / reviewer / approver、承認 proof、retry / manual_required、raw 値非記録ルールを整理しただけで、CLI runner、admin UI、DB audit table、real destructive deletion は追加していない。
- RR-3h として、内部 operator CLI skeleton `npm run account-deletion:operator` と self-test `npm run account-deletion:operator:self-test` を追加した。CLI は dry-run default、1 stage per invocation、safe JSON output、request ref 非再掲、proof path / latest dry-run / prior stage satisfied の guard modeling を行うが、RR-3h では actual stage services に接続せず `actual_service_not_connected_in_skeleton` で blocked にする。public UI/API、DB migration、real destructive deletion は追加していない。
- RR-3i として、operator CLI に fake-first stage service seam を追加した。`runAccountDeletionOperator` は injected fake service を受け取れるが、通常 CLI は service 未接続のままで、guard が欠けると fake service も呼ばない。service result は safe summary に正規化し、raw user / provider / storage / DB / Auth data は出さない。real provider / Storage / DB / Auth cleanup には接続していない。
- RR-3j として、RR-3b〜RR-3i の final safety review を `docs/rr-3j-final-safety-review.md` に固定した。stage order、guard、failure / retry / manual_required、proof / raw data policy は一貫しているが、実 service 接続前には operator request ref を server-side で `userId + deletionRequestId` に安全解決する wrapper と、real service result を safe summary に通す追加 self-test が必要。destructive guard、real service connection、disposable proof、real provider / Storage / DB / Auth cleanup は実行していない。
- RR-3k として、operator CLI に safe request resolver seam を fake-first で追加し、`docs/rr-3k-safe-request-resolver-wrapper.md` に責務を固定した。通常 CLI は resolver / real service 未接続のままで、destructive guard や prior-stage guard が欠けると resolver も service も呼ばない。self-test では fake resolver が operator request ref を internal-only `userId + deletionRequestId` に解決し、fake service result は safe summary sanitizer を通ることだけを確認した。real DB lookup、real service connection、destructive guard、disposable proof、real provider / Storage / DB / Auth cleanup は実行していない。
- RR-3l として、operator CLI の `status` / `summary` stage に read-only request resolver を接続し、`docs/rr-3l-read-only-request-resolver-status-proof.md` に責務を固定した。resolver は account deletion request UUID または `anonymized_user_ref` を server-side で読み、request status / cleanup stage status / retry count だけを safe summary として返す。raw user id、email、deletion request id、request ref は出力しない。provider / Storage / DB / Auth の destructive stage には real service 接続しておらず、destructive guard、disposable proof、real cleanup は実行していない。
- RR-3m として、disposable-account destructive proof の対象選定と proof preparation を `docs/rr-3m-disposable-proof-request-selection.md` に固定した。operator CLI の `status` / `summary` には `--proof-candidate-*` flags による fake-first candidate 判定を追加し、confirmed request、fresh stage status、disposable / owner / reviewer / approver / dry-run / Human Check alignment の safe confirmation だけで `pass` / `blocked` を出す。raw id や raw data は出さず、destructive guard、real service connection、disposable proof、real cleanup は実行していない。
- RR-3n として、disposable proof 実走前の fake-only proof log generation / final operator rehearsal を `docs/rr-3n-fake-only-proof-log-rehearsal.md` に固定した。`npm run account-deletion:operator:rehearsal` は provider -> Storage -> DB -> Auth -> completion の fake sequence を safe JSON / markdown で出力し、operator / reviewer / approver / environment / proof template は `provided_not_echoed` marker にする。destructive guard が有効なら rehearsal は `BLOCKED` になり、real provider / Storage / DB / Auth cleanup service は呼ばない。
- RR-3o として、RR-3n の fake-only rehearsal output を RR-3f proof template に転記しやすい sample proof package `docs/rr-3o-sample-disposable-proof-package.md` にまとめた。metadata、stage decisions、safe counts、safe reason codes、PASS/WARN/BLOCKED/FAIL、raw data absence、fake-only と real destructive proof の違いを固定し、`npm run account-deletion:proof-package:self-test` で sample artifact に raw-looking id / secret / URL が無いことを確認できる。real disposable proof、destructive guard、real cleanup service は実行していない。
- RR-3p として、future disposable live proof 前の disposable account fixture / dry-run proof checklist を `docs/rr-3p-disposable-fixture-dry-run-proof-checklist.md` に固定した。disposable account の条件、app data / Storage / ElevenLabs provider fixture、provider / Storage / DB / Auth dry-run 順序、GO / WARN / BLOCKED / FAIL、raw data 非記録ルールを整理しただけで、disposable account 作成、destructive guard、real cleanup service、DB schema / migration は変更していない。
- RR-3 / Gate 1 の現状棚卸しを `docs/rr-3-gate1-current-status-inventory.md` に追加した。RR-3b〜RR-3p の completed / docs-only / fake-self-test only / read-only / 未実行 / human-check backlog を整理し、real destructive deletion は未実行、destructive guard は未有効化、次に進む候補は RR-3q か final Human Check Backlog batch として整理した。
- RR-3q として、disposable destructive proof 実走前に Codex だけで残せる final non-destructive evidence check を `docs/rr-3q-read-only-disposable-request-status-rehearsal.md` に固定した。operator `status / summary`、stage dry-run checklist、fake-only rehearsal、sample proof package、production preflight、Storage/RLS checker の実行順と proof 転記先を整理しただけで、disposable account 作成、destructive guard、real cleanup service、public UI/API、DB schema / migration は変更していない。
- RR-3r として、将来の disposable live proof 用の空 proof package template を `docs/rr-3r-future-disposable-proof-package-template.md` に追加した。RR-3q evidence flow、RR-3o fake sample との差分、stage-by-stage actual proof 欄、raw data 非記録ルールを固定しただけで、fake / real deletion、destructive guard、real cleanup service、public UI/API、DB schema / migration は変更していない。
- Gate1k として、Web beta 前の final no-go checklist / human-check execution packet を `docs/gate1k-web-beta-final-no-go-checklist.md` に追加した。2026-05-11 の Gate1l acceptance と Azure live smoke PASS を反映し、Final Human Check `BLOCKED` は現時点でなし。Web beta decision は full `GO` ではなく `GO WITH WARNINGS` として整理した。実デプロイ、real destructive deletion、DB schema / migration は実行していない。
- Gate1l として、`GO WITH WARNINGS` を Web beta release candidate 判断に近づけるための owner acceptance / Azure live smoke checklist を `docs/gate1l-go-with-warnings-acceptance-azure-smoke.md` に追加し、人間確認結果を Gate1i / Gate1k / Human Check Backlog に反映した。release owner / deployer / rollback owner / incident owner / post-deploy smoke owner / provider monitoring owner / support-deletion owner は app owner。Azure pronunciation live smoke は `record -> evaluate -> review -> progress` が PASS。OpenAI Default project、OpenAI app-side cost limits / kill switch、ElevenLabs manual monitoring / cache reuse / kill switch、legal/support beta draft は WARN として release owner が small-cohort Web beta では受容した。実デプロイ、real destructive deletion は実行していない。
- listen / record では、script content から表示用の practice chunks を導出し、意味の塊・息継ぎ・語尾まで言い切る cue を見ながら練習できる。DB schema / API contract / cache は変えない。
- review では、weak words と practice chunks を表示用に照合し、次に練習する意味の塊を 2〜3 件以内で出す。該当 chunk が無いときは最初の chunk で区切りと語尾を整える fallback guidance を出す。
- record は短すぎる録音を警告し、mock transcription では開発用 transcript なしで進めない。
- 結果確認では認証付き `/api/takes/[takeId]/audio` から保存済み録音を再生できる。
- `setup/voice` は、お手本ボイスが必要なときだけ開く設定画面として扱い、voice 準備後に候補の script で `listen` に入るか、新規 script を作るかをその場で選べる。
- `/` は practice-first Home として、今日の練習、前回の続き、最新結果、progress への導線を先に出す。
- `scripts` は Practice library として、まず今日の練習を 1 本選び、`listen / record / script 複製 / 最新結果` を script ごとに選べる。不足前提があるときは、先に `voice 設定` や `listen` 側へ戻す。
- `scripts` は page 上部で `初回導線 / 再開導線` を先にまとめ、複製は card 側のその他の操作へ寄せている。
- `voice 設定` に戻る導線は、候補 script が見えている場面ではその script の `listen` に戻る形を優先している。
- `scripts` は練習候補を選ぶ入口、`progress` は最大5本の練習スロットから成果を選んで見る入口として扱う。
- `setup/voice` は `?next=` が internal path のときだけ保持し、blocked state から戻る設定・管理導線に使う。
- `/api/uploads/voice-consent` を追加し、同意録音は `voice-consents` bucket の app-owned object として先に保存してから `/api/voice-consent` に参照を渡せる。
- `setup/voice` / `scripts` / `listen` / `progress` が参照する consent と default voice は、現在の `VOICE_PROVIDER` に一致する row だけを見る。
- `setup/voice` と voice service は、provider 名の直書きより `provider requirements` を本線にしている。`requiresConsentRecording / requiresSampleAudio / requiresProviderConsentId / entitlementSensitive` のような条件は factory から読む。
- voice provider 本接続時も固定前提なのは、app-owned の `voice_consents / voices / script_audios` row、app-owned storage (`voice-consents / voice-samples / script-audios`)、server 側 cache key 判定、replay route 経由の protected 再生、の 4 つ。
- progress は5本までの練習スロットを切り替え、選んだ台本の最新結果 / ベスト結果 / 保存済み録音を確認できる。必要なら `listen / record / 結果確認` に戻れる。
- Azure Speech pronunciation assessment は live manual smoke で wav/PCM 録音に対して `record -> evaluate -> review -> progress` が通り、さらに OpenAI transcription + Azure pronunciation evaluator の実ブラウザ人間マイク録音でも review / progress まで確認済み。
- core path の最小 Playwright smoke はあり、少なくとも `未ログインでは protected route に入れない`、`mock voice で setup/listen`、`listen blocked branch の provider_unavailable / consent_required / voice_required`、`provider_unavailable でも保存済みお手本ボイスがあれば listen を継続でき、audio 要素が playback-ready までロードできる分岐`、`mock listen 生成`、`mock 前提の record -> review -> progress` は自動で崩れを拾う。
- `npm run e2e:auth-guard` と `npm run e2e:setup-voice` を分けて回せるので、manual smoke 前でも guard と voice setup の current flow を短く確認しやすい。

## 壊してはいけない前提
- canonical source は server 側に置く。必要な script / result は owned data を引き直す。
- `record -> upload -> transcription -> evaluate -> save` の流れを崩さない。
- review 保存の atomicity を崩さない。
- `recordings` は owned な `user_id/script_id/...` key に閉じる。
- `script_audios` の cache semantics を崩さない。
- voice 周辺は `consent -> voices -> default voice -> script_audios` の app-owned flow を維持する。
- provider bytes は `speakScript` 側で app-owned replay 参照へ正規化してから `script_audios` に保存する。
- `script_audios.storage_path` は provider 直 URL ではなく、listen が認証付き replay route 経由で解決する app-owned 実体参照として扱う。
- replay route は `script_audios.storage_path` から owned なお手本ボイス実体を引き直して返す。
- app-owned お手本ボイス bytes の保存先は、現 repo では Supabase Storage の専用 bucket `script-audios` を本線にする。`recordings` と混ぜない。
- object key は `<userId>/<scriptId>/<voiceId>/<cacheKey>.<ext>` を本線にする。user prefix は coarse ownership 境界、script/voice/cacheKey は cache 実体の整理に使う。
- storage policy は public にせず、replay route から authenticated route client で server-side download する前提に寄せる。
- policy 側は bucket 名と user prefix までの coarse 制御に留め、script/voice/cache の owned 整合は app 側の `script_audios` lookup で担保する。
- `script_audios` には `stored_asset` を追加し、`storageBucket / storageObjectKey / contentType / byteLength` を永続化する。
- `provider_voice_id` は provider 呼び出し用の外部 ID に留め、cache や replay ownership の基準にしない。
- `takes` は script 全文 snapshot を持たないので、in-place script edit は軽く入れない。
- 履歴意味が絡むときは script mutation より duplication を優先する。
- UI/UX small diff でも、DB schema / migration、API contract、auth / ownership / storage access、provider 本接続、Listen fixed audio bar、main loop、UI direction docs に影響する場合は停止して報告する。

## 直近の変更
- Listen の主見出しと主CTAは `お手本ボイスを聞く` / `お手本を聞く` に寄せ、`Native voice` は小さなラベルとしてだけ使う。音声ロジック、protected replay、下部固定 audio bar は変えていない。
- Record は録音直後の local blob / 選択ファイルを「この Take を聞く」としてその場で preview できる。Review は first view で保存済み録音を聞けるようにし、Progress は選択 slot の台本全文を最新 / ベスト Take の近くで確認できる。録音 upload、evaluate、review persistence、progress aggregation、API contract は変えていない。
- Home は「今日の1分スタジオに入る入口」として、studio entrance / flow preview / control panel CTA / settings surface の役割を整理した。hero 内の flow preview は押せる CTA に見えないよう弱め、主CTAは「1分を始める」に一本化している。auth、navigation、各画面の main loop logic は変えていない。
- Progress の slot 選択は warning に見えない warm paper / darker border / `選択中` label へ寄せた。`?scriptId=` で開いた script は5枠表示に含め、詳細エリアは選択 script に紐づく latest / best / saved recordings だけを表示し、無関係な全体 latest / best は混ぜない。
- Progress は「声のログを見る場所」として、5スロット選択 / 最新 Take / ベストテイク / 保存済み録音 / 保存済みお手本を log shelf / take paper / control panel に寄せた。progress aggregation、best take logic、canonical source は変えていない。
- Review は「今回の Take から次の1点を見つける場所」として、summary / score / Focus words / コーチメモ / 次の操作を小さな録音ブースの surface system に寄せた。score は残しつつ通知表っぽく主役にしすぎず、review data / canonical source / audio replay logic は変えていない。
- Record は「今日の Take を残す場所」として、hero / 今日の台本 / 録音パネル / 録音前の区切り / 今回の Take 表示を小さな録音ブースの surface system に寄せた。開発用 transcript 入力は mock 確認時だけにし、録音、upload、evaluate、review 遷移ロジックは変えていない。
- Listen は「お手本のリズムを耳に入れる場所」として、hero / お手本ボイス area / script paper / 区切り表示 / 下部固定 audio bar を小さな録音ブースの surface system に寄せた。単一 audioRef、protected replay URL、seek / play / pause の音声ロジックは変えていない。
- `/scripts/new` は「今日の台本を用意する場所」として、入口を studio surface、テンプレートと英文入力を script paper、ヒントを coach note、保存 / AI生成の action area を control panel に寄せた。script 作成 API / contract / 保存後の listen 遷移は変えていない。
- listen の音声再生は、client-side fetch で Blob URL 化せず、単一の audio element が protected replay URL を直接読む形へ戻した。`/api/script-audio/[audioId]` は Range request に 206 で返せるため、mobile Safari でも metadata / seek / play が通りやすい。
- listen の見えている音声操作は下部固定 audio bar に一本化した。hidden audio element は単一の audioRef を維持し、再生 / 一時停止は音声 URL があれば押せ、3秒 / 5秒の seek は metadata 準備後に有効になる。
- setup/voice は「お手本を聞けます」「次の入口」などの余分な状態 section と provider/debug/readiness 表示を主導線から外し、現在の声の状態、アップロード、再アップロードだけに絞った。
- Home から「毎日使う練習場です。細かい設定より、まず練習を選びます。」の説明を削った。
- listen は大きな sticky audio card や区切り文ごとの重複 controls をやめ、区切りカードは英文 / cue / focus に集中させた。音声操作は下部固定 bar で行う。
- scripts は hero の重複した「練習を始める」ボタンを削り、保存済み練習カードを選ぶか、新しい1分を作るかに整理した。
- 主要操作では、pressed / tap feedback、loading aria、録音開始中の表示、ファイル選択済み表示を足し、処理が遅い場面でも押したことが分かるようにした。
- Home は Practice / Progress への入口に寄せ、練習一覧の重複表示をやめた。Practice は5本までの練習スロット選択、Progress は成果確認に役割を分ける。
- 共通 nav は pathname ベースで現在ページを点灯する。Home / Practice / Progress / Voice / Settings を対象にする。
- listen の区切り表示では、各 chunk の英文 / cue / focus だけを出す。正確な区切り音声切り出しはタイムスタンプがないため未実装で、聞き直しは下部固定 audio bar で行う。
- setup/voice は登録済みの自分の声、クローン元音声の有無、同意録音 metadata から分かる形式/サイズだけを表示し、既存 voice 作成 flow で再アップロードして作り直せる。
- Brush-up v1 は `docs/brush-up-v1-design-plan.md` に設計だけを固定した。既存お手本を上書きせず、Review 起点の candidate から始める案だが、storage identity / cache key / metadata contract は実装前の判断点として残す。
- listen / record / progress は説明ページではなく操作ページに寄せた。`今やること` / `次にやること` / 長い guidance は主導線から外し、必要操作だけを残す。
- listen は sticky な お手本ボイス player、スクリプト、区切り表示、録音へ進む、お手本ボイスを作り直す、に絞った。3秒 / 5秒の巻き戻し・早送りを追加し、区切り練習中も player を見失いにくくした。
- record はマイク録音、お手本へ戻る、録音前の区切り、録音確認、評価へ進む、ベータ10回表示に絞った。音声ファイル利用と開発用入力は details 側に下げた。
- progress は最大5本の練習スロットを切り替え、選んだ台本ごとに最新結果 / ベスト結果 / 前回結果 / 保存済みベスト録音 / 保存済みお手本を確認する画面にした。
- progress の自分の録音と保存済みお手本は protected audio player 経由で取得し、raw protected URL を audio に直接渡さない。
- empty / not-found / fallback / config-error 状態でも、main loop に戻りやすい section 構成へ寄せている。
- `progress` の no-result 状態や `record` の録音前状態でも、`listen に戻る / そのまま進む / 録音を作り直す` をその場で選びやすくした。
- `setup/voice` / `scripts` / `progress` では、上流の handoff で次に開く script や route を選びやすいように小さい運用導線を足した。
- `scripts` / `progress` は、voice 前提が不足しているときと record 前提が不足しているときを分け、まず直すべき不足前提を launchpad 側で読みやすくした。
- `scripts` 一覧は各 card に `最新結果の要点` を足し、一覧のまま再開判断に必要な score / 弱点語 / coach を拾えるようにした。
- `progress` は `保存済み結果 / 最新結果 / 結果確認` の語彙を detail 側へ寄せ、画面をまたいだ説明の読み替えを減らした。
- `progress` の `最新結果 / ベスト結果` card も、detail / status card と同じ要点 block を使って保存時刻・弱点語・coach nextStep を確認できるようにした。
- `review` も `この結果 / 最新結果 / ベスト結果 / 次の一手` の読み方を progress 側へ寄せ、detail だけ見ても比較と次の戻り先が迷いにくいようにした。
- `progress` の `強み・弱点語・coach` と `最新とベストの差` でも、review と同じ比較軸で読めるように current/best coach と strengths の見せ方を寄せた。
- `listen` は voice 設定が不足しても、保存済みお手本ボイスがある場合はそのまま確認を続けられる。
- `record` は transcription 前提が不足しているとき、開発用 transcript 入力ではなく `listen / scripts / voice 設定` 側へ戻る recovery を優先する。
- `setup/voice` は、ready state の主ボタンを 1 本に保ちつつ、復帰導線と候補 script の切り替えは ready block 側で扱うようにした。
- `listen` / `record` / `review` は、`最新結果を見る` などの設定・管理導線と script 単位の current step を足し、履歴中の 1 件を開いたときでも通常利用へ戻りやすくした。
- `record` は対応形式とサイズ上限を client 側でも先に案内し、unsupported / oversize を upload 前に止めるようにした。
- `progress` は「次に戻る 1 本」を主導線に保ち、直近で見ていた結果からの再開は `Other actions` 側に寄せた。
- main loop 周辺の dynamic route/page は `params` を非同期解決でそろえ、`listen / record / review / 保存済み録音再生` で route 起因の 404 や再生詰まりが出にくいようにした。
- `listen` は protected audio の blob URL 準備ができるまで空のプレーヤーを出さず、初回からこの画面だけで再生しやすいようにした。
- 語彙は `保存済み結果 / 最新結果 / ベスト結果 / 音声状態 / 開発用 transcript / 次の1本` を基準に日本語中心でそろえている。
- Visual Art Direction Phase 1 では、global design tokens と layout / nav を「スマホの中にある小さな録音ブース」に寄せている。page chrome は booth wall / studio surface / control panel を基準にし、white / near-white は page shell ではなく script / take paper として使う方針。mint は dominant UI color には戻さない。
- Visual Art Direction Phase 2 では、`/scripts` を「今日の台本を選ぶ棚」として整理している。slot count は shelf indicator、script card は shelf item、本文抜粋は script paper、最新 Take は coach note / inset として扱う。

## 未解決論点
- 評価が甘く見える件は `docs/evaluation-calibration-options.md` に調査メモを追加した。mock evaluator は word coverage ベースで発音品質そのものを見ないため高く出やすく、Azure evaluator は provider の生スコアを大きく補正せず保存している。まずは DB / API / provider 変更なしで、mock 時の `簡易評価` 表示、`参考スコア` ラベル、Focus words / 次はここだけの強調から検討する。
- Evaluation calibration の first small diff として、Review / Progress の visible score label を `参考スコア` / `目安` に寄せ、mock provider 環境では `練習用の簡易評価` と短く注記する。score 算式、persisted score、best take 判定、API response、DB schema、provider 実装は変えない。
- Script Studio の設計は `docs/script-studio-plan.md`、`docs/script-studio-freeze-quota-plan.md`、`docs/script-studio-voice-generation-preflight-plan.md`、`docs/script-studio-quota-event-plan.md` に固定した。Phase S1 の local contract / type / pure helper、Phase S2 の `/scripts/new` UI mock、Phase S2.5 の generation contract / validation / freeze boundary、Phase S2.6 の provider / pipeline boundary、Phase S3a の OpenAI adapter、Phase S3b の server route / service boundary、Phase S3c の `/scripts/new` draft preview 接続、Phase S3d の draft-to-form copy、Phase S3e の freeze/quota 設計、Phase S4a の表示用 freeze preflight preview、Phase S4b の saved script read-only check、Phase S4c の saved-script-only decision、Phase S4d の voice generation preflight design、Phase S4e の listen preflight-only UX copy、Phase S4f の quota event design、Phase S4g の provider/cache UI refinement、Phase S4h の quota preflight copy only、Phase S4i の copy density review、Phase S5a の quota event schema design、Phase S5b の text generation quota event write path design、Phase S5c の voice generation quota event write path design、Phase S5d の implementation readiness design、Phase S5e の text generation quota event implementation plan、Phase S5f の text generation quota event first implementation、S5f-db-smoke-plan の dev DB / mock smoke checklist、Phase S5g の voice generation quota event implementation plan、Phase S5h の quota_events voice schema extension plan、Phase S5i の voice quota schema extension first implementation、Phase S5j の speakScript non-blocking voice quota event connection、S5k-plan の text + voice DB smoke checklist consolidation は追加済み。`script_freezes` table、freeze 保存、quota enforcement、voice generation gating は未実装。
- Audio Library の設計は `docs/audio-library-plan.md` と `docs/audio-library-migration-plan.md` に固定した。MVP 推奨 scope は script-scoped library で、saved model audios 最大 5 件 / saved best takes 最大 5 件。`script_audios` と `takes` は canonical source のままにし、Audio Library は curation layer として扱う。S6c で `script_saved_model_audios` / `script_saved_best_takes` の migration、client select-only RLS、service-role write 前提の service skeleton を追加し、S6d/S6e で model audio と best take の list / save / label update or explicit slot replacement / unsave API を追加した。0011 適用後の API smoke では両 API の save / duplicate / slot validation / patch / replace / delete、RLS spot check、metadata privacy、underlying audio/take/storage preservation を確認済み。S6f で listen の現在再生できる見本音声を Audio Library に保存 / 保存解除できる小さな UI、S6g で review の現在の録音 take をベスト保存 / 保存解除できる小さな UI、S6h で `/progress` の script card に保存済み見本音声 / ベスト録音の件数と最小再生・review 導線を追加した。S6i smoke では listen -> save model audio、record -> review -> save best take、progress summary、replay route、review link、unsave 後の underlying data preservation を確認済み。
- speed / voice style の設計は `docs/speed-voice-style-plan.md` に固定した。`playbackRate` は再生速度だけ、生成時の `target_speed / target_wpm / pause_density / voice_style_preset` は音声 identity と練習目的に関わる値、Script Studio readiness は script 自体の話しやすさを整える値として分ける。S7b では拡張 preset 定義と provider mapper 境界だけを追加し、S7c では既存4 preset の copy / compatibility alias、S7d では listen の生成 style / 再生速度 copy、S7f では listen の情報密度と操作直後の pending feedback、S7g では saved model audio metadata snapshot 表示、S7h では mock 前提の provider mapping boundary smoke、S7i では lightweight static smoke script を整理した。多彩な preset UI や DB schema 変更はまだ進めていない。
- 実 provider live smoke / 再 smoke の準備手順は `docs/voice-provider-smoke-plan.md` に分けた。ElevenLabs はユーザー実ブラウザで clone voice 作成、script TTS、cache hit quota row、4 style の別 cache row、fresh Audio Library metadata、privacy spot check まで確認済み。S8e で failure branch / cleanup policy も固定済み。OpenAI voice は entitlement-sensitive な追加確認 provider で、v1 practice mainline には出さない。
- voice provider は mock fallback を維持しつつ、ElevenLabs は `setup/voice -> listen -> cache reuse -> Audio Library fresh save -> 4 style cache identity` の実 provider 経路が通った状態。failure branch の意図的 live smoke と provider cleanup API は残っている。
- 次の product 本線は OpenAI voice ではなく Script Studio と practice UX 改善。`/scripts/new` は4入口から始められるが、最終保存は既存 manual form と `/api/scripts` の flow に集約する。
- mock voice でも `stageScriptAudioForReplay` は短い wav を `script-audios` bucket へ upload してから replay 参照を保存する。`/api/script-audio/[audioId]` は `stored_asset` から server-side download する。
- `provider.synthesize` の受け口は `stageScriptAudioForReplay` 側に寄せ、`inline-bytes` はそのまま保存、`temporary-url` は今後の差し替え点、current mock は `mock-replay-path` で app replay path を渡す。
- 実 provider adapter は最小でも `providerRequestId` と `audioSource` を返す。`playbackPath` は任意で、無ければ `stageScriptAudioForReplay` 側が app replay path を導出する。
- content type の正規化は provider adapter helper 側で先に行い、`stageScriptAudioForReplay` は正規化済み `audioSource` を storage upload に使う。
- `temporary-url` の generic server-side fetch はまだ入れていない。現在の repo では、許可する host と fetch 方針を固定しないまま replay service から外部 URL を取りに行かない。
- mock 前提でまだ仮置きなのは、`sampleAudioPath` fallback、provider-side consent endpoint の要不要、verification pending の扱い、provider failure copy の細部、のような adapter 近傍の差分に留める。
- 最初の実 provider adapter は `inline-bytes` を本線にする。`temporary-url` は追加の server-side fetch が必要なので、次の差し替え点として残す。
- adapter helper に最低限渡すのは `providerRequestId / bytesBase64 / contentType`。`contentType` が generic すぎると object key 拡張子と replay header が弱くなるので、provider から具体 mime を取れる実装を優先する。
- 次の実装本線は ElevenLabs IVC に寄せる。`providers/voice/elevenlabs.ts` は app-owned sample audio を `/v1/voices/add` へ渡し、response の `voice_id` を保存し、`/v1/text-to-speech/:voice_id` の bytes を `inline-bytes` に戻す。
- `providers/voice/openai.ts` の `synthesize` は `POST https://api.openai.com/v1/audio/speech` を叩く最小実装まで入った。request body は `model / input / voice / response_format` に絞り、response bytes と `x-request-id` / `content-type` header を `inline-bytes` に落とす。
- OpenAI 側で `content-type` が弱い場合は `response_format=wav` を fallback にして `audio/wav` へ寄せる。`bytesBase64` 変換は provider adapter 内で行い、replay service へは渡さない。
- `VOICE_PROVIDER=openai` は `OPENAI_API_KEY` と `SUPABASE_SERVICE_ROLE_KEY` がそろっていれば supported として扱う。`createConsent` は app-owned consent recording を server-side download して OpenAI multipart request に載せ、`createVoice` も app-owned sample audio を download して multipart request に載せる。
- OpenAI voice を追加確認する場合の backend log は、同意録音 download / voice create / synthesize reject / response invalid を request id と failure point 付きで追える。
- `VOICE_PROVIDER=elevenlabs` は `ELEVENLABS_API_KEY` と `SUPABASE_SERVICE_ROLE_KEY` がそろっていれば current contract 上で `createVoice / synthesize` を使える。consent は provider-side endpoint を要求せず、app 側の consent row だけを canonical step として扱う。
- setup/voice の Provider readiness では、provider-side consent endpoint を使うか、synthesize が `inline-bytes` 本線か、verification pending を保存しないか、まで diagnostics で確認できる。
- `setup/voice` は `provider diagnostics` を表示し、ElevenLabs では env / sample audio / verification pending / TTS model のどこを確認すべきかを画面で読める。
- ElevenLabs はユーザー実ブラウザ確認で、`sample upload -> voice clone -> protected replay 経由の listen TTS` まで成功済み。別環境で再確認するときや cache/style/metadata/失敗 branch を見るときは、`docs/voice-provider-smoke-plan.md` の手順に沿う。
- 実 provider manual smoke の前に `npm run voice:preflight` を使うと、現在の `VOICE_PROVIDER` と必要 env の不足だけを repo 内で先に確認できる。entitlement や live response まではここでは確定しない。
- `voice-consent` は `accepted` を残しつつ、`name / language / recording` を受けられる。upload 済み recording は `voice_consents.metadata` に参照を保存する。
- OpenAI voice の create には local consent row の `metadata.providerConsentId` を使う。local `consent.id` 自体を provider へ渡さない。
- OpenAI voice では raw `sampleAudioPath` fallback を provider へ流さず、app-owned な `storage://voice-samples/...` 参照だけを sample input として通す。
- OpenAI voice endpoint が `Your organization does not have access to this endpoint.` のように失敗した場合は、auth/upload 実装失敗ではなく entitlement 不足として扱う。server log では provider status / request id / detail を残し、UI では `VOICE_PROVIDER=mock` に戻すか entitlement 付与後に再開する判断を出す。
- entitlement 不足時の実運用 fallback は、`.env.local` で `VOICE_PROVIDER=mock` に戻して再起動し、`/setup/voice` から mock voice を作成して main loop 開発を続ける流れを本線にする。
- `setup/voice` は sample audio を `createVoice` に直送せず、先に `/api/uploads/voice-sample` で app-owned に保存してから `createVoice` へ参照を渡す。`recordings` とは lifecycle が違うので bucket は `voice-samples` を分けた。
- `createVoiceRequestInput` は `sampleAudio?: { audioPath: string; contentType?: string; byteLength?: number }` を本線にし、旧 `sampleAudioPath` は互換 fallback として段階的に縮退させる。
- provider 共通項目は `consentId / label / sampleAudio`、provider 固有項目は `providerOptions` のような入れ物へ閉じ込める。OpenAI voice の追加確認でも provider 固有項目を増やさず、uploaded sample 参照だけで足りる形を優先する。
- OpenAI 固有の upload / multipart / entitlement detail は `providers/voice/openai.ts` に寄せ、setup/voice と service は `provider requirements` を見て UI と validation を分岐する。将来 ElevenLabs などを足すときは、まず factory の requirements と provider adapter を差し込む。
- `providers/voice/elevenlabs.ts` は current contract のまま `createVoice / synthesize` を実装し始めている。`createVoice` は app-owned `storage://voice-samples/...` を ElevenLabs `/v1/voices/add` multipart へ変換し、`synthesize` は `/v1/text-to-speech/:voice_id` の bytes を `inline-bytes` に戻す。
- current repo の ElevenLabs synthesize は `output_format=mp3_44100_128` を使い、provider bytes を protected replay 用の app-owned storage へ載せ替えて listen へ返す。
- ElevenLabs の見本音声 style preset は `script_audios.cache_key` に含め、style が違う見本音声を同じ cache として混ぜない。DB schema は変えず、cache key 文字列だけで分ける。
- ElevenLabs では OpenAI のような provider-side consent endpoint を current repo へ持ち込まず、`createConsent` は app 内同意ステップを保つための local acknowledgment として扱う。`providerConsentId` を後続 request に使わない。
- ElevenLabs `createVoice` response で verification が必要な voice は、current repo では pending state を保持しないので fail-fast する。verification を要求された場合は ElevenLabs 側で完了してから再試行する。
- ElevenLabs 実接続前の主な failure point は `sample download / voice clone reject / verification required / synthesize reject` に分けて扱う。server log では request id と failure point を残し、UI では auth/upload バグと誤認しない recovery copy を出す。
- `sample_audio_path` は当面、upload 済み sample の `audioPath` か旧 fallback path を保存する互換カラムとして使う。ownership 判定は `voice-samples` の owned reference 側で先に行う。
- app-owned 見本音声実体の保存先は Supabase Storage を本線にしており、mock でも `script-audios` bucket / coarse policy / `stored_asset` / upload/download 配線を通す。未着手なのは cleanup 運用と実 provider bytes への差し替え。
- 見本音声 bytes の最小 metadata は `script_audios.stored_asset` に `storageBucket / storageObjectKey / contentType / byteLength` を入れる。`duration_seconds` はあれば補助になるが replay の必須前提ではない。
- 一時ファイルやメモリだけで持つ案は、persisted cache と複数 request / instance 前提に合わないので本線にしない。
- voice の安全な fallback は引き続き `VOICE_PROVIDER=mock`。ElevenLabs は実 provider 経路が一度通ったが、OpenAI voice は entitlement 待ちで停止中。
- pronunciation evaluator は `PRONUNCIATION_PROVIDER=mock` を開発 fallback に保ちつつ、Azure Speech pronunciation assessment は local WAV と実ブラウザ人間マイク録音で live smoke 済みになった。
- pronunciation evaluator は `record -> evaluate -> save` の末端ではなく、record / progress 側でも guard して扱う。Azure を選んだだけで録音保存後に初めて詰まらないよう、env 不足・未対応音声形式・接続失敗を前段で切り分ける。
- Azure evaluator の env 前提は `AZURE_SPEECH_KEY / AZURE_SPEECH_REGION`。record では provider diagnostics を表示し、未設定時は明示的に blocked にする。
- current repo の Azure evaluator は wav / PCM を前提にする。browser 録音や非 wav file は、client 側で decode できるときだけ wav / PCM へ正規化してから upload する。変換できない環境では wav file を選び直す。
- Azure evaluator は continuous recognition で 1 分前後の録音を評価し、script locale を recognizer に渡す。prosody は en-US のときだけ有効にし、UI 上の rhythmScore へ寄せる。
- evaluator が未接続または Azure 側で失敗したときは、record / progress の `Recovery plan / Next action` で listen や scripts に戻し、開発継続は `PRONUNCIATION_PROVIDER=mock` に戻す案内を本線にする。
- 実 transcription は `TRANSCRIPTION_PROVIDER=openai` のときだけ。
- progress / 結果確認は現在の script 本文を表示しており、`takes` に当時の script snapshot はない。
- 署名付き再生 URL は設計に入れていない。
- login 完了後の continuity は、存在する app page パターンだけを通す。path が無い・不正なときや、`/api/*` / `/_next/*` / `/auth/*` / replay API URL / 存在しない internal path は `/scripts` に fallback する。
- page 内の `next` も internal path だけを受け取り、安全でないときは各画面の fallback route に戻す。
- `/login` でも `next` が有効・無効・未指定のどれかを短く表示し、送信後メッセージと callback 後の戻り先説明がずれないようにしている。
- `/scripts` / `/setup/voice` / `/progress` / `/settings` は page 側の redirect だけでなく middleware でも未ログインを止める。sign-in では Supabase へ素の `/auth/callback` を渡し、戻り先 continuity は短命 cookie で保持する。
- sign-in / callback / sign-out と setup/voice 直後の auth 必須 API は、Supabase が返す cookie 変更を route response に明示反映する。client fetch も same-origin credentials 前提でつなぐ。
- `/settings` には通常の logout 導線を置く。Gate 0 human smoke では、既存 session から logout し、新しい magic link で `login -> callback -> /scripts` を確認する。
- logout と成功した callback 後は `nm-login-next` と PKCE verifier cookie を明示的に掃除する。callback 失敗時は、古い / 期限切れ link で次の pending login を壊さないよう PKCE verifier cookie は残し、login 画面で理由を分けて再試行させる。`next` は internal path のみ受け取り、既知 route prefix は小文字へ正規化する。
- Gate 0 smoke で magic link を連打すると Supabase Auth の email rate limit に当たることがある。その場合は callback bug と混同せず、しばらく待ってから新しい link を発行する。UI では rate limit を raw detail なしの日本語 message として表示する。
- `/auth/callback` と `/api/auth/*` は middleware の auth 初期化を通さず、PKCE verifier cookie と callback exchange の間に余計な auth 読み出しを挟まない。
- callback は `code` 交換と `token_hash` 検証の両方を受ける。失敗時は、`PKCE verifier cookie が callback に届いていない` 場合と、`cookie はあるが Supabase exchange 自体が失敗した` 場合を分けて login に戻す。server log では `Auth callback exchange failed` に cookie 有無も残す。
- auth の magic link redirect URL は受信 Host と forwarded proto から origin を組み立てる。callback 後の戻り先は相対 `Location` にして、`127.0.0.1` と `localhost` の host ずれで PKCE cookie が落ちないようにする。
- OpenAI voice は manual smoke で provider-side consent 登録段階から entitlement 不足らしい失敗に到達した。repo 側の main flow 実装はここまでで止め、v1 mainline は ElevenLabs / mock fallback で続ける。

## 次に狙う方向
- Performance audit は `docs/performance-audit.md` に固定した。現時点の主な速度懸念は巨大 bundle ではなく、`getProgressOverview()` の全履歴 hydrate が複数画面で走ること、Review の全履歴読み込み重複、Progress の slot 別 Audio Library query、protected audio の blob 化待ち、`upload -> storage download -> transcription -> pronunciation -> save` の同期待ちにある。
- Development-only timing は `lib/performance/timing.ts` に入れた。`NODE_ENV !== "production"` では自動で `[timing]` log を出し、本番では `NATIVE_MINUTE_ENABLE_TIMING=1` の短時間計測時だけ出す。label には user id / script 本文 / transcript / storage path / raw audio / provider payload を入れない。
- Authenticated timing の測定順と判断基準は `docs/performance-audit.md` の runbook に固定した。`npm run performance:timing-smoke` は既存 storageState があれば protected route を叩けるが、結果は HTTP status / duration だけに留め、実測値は server console の `[timing]` label を読む。
- 2026-05-26 の local authenticated timing では、mock provider 前提で Progress の Audio Library read、evaluate の storage download / persistence、protected audio replay、Review の複数 loading path が目立った。詳細は `docs/performance-audit.md` の timing run を見る。
- Progress Audio Library deferral / selected-slot scope として、`/progress` の初期表示では選択中 script の保存済みお手本 / ベスト録音だけを読むようにした。slot 選択、latest / best、保存済み録音の表示意味は維持し、DB / API / auth / storage policy は変えていない。
- Listen / Record 向け selected-script summary として、両画面の初期表示では全 script の `getProgressOverview()` を読まず、対象 script の take / weak words / coach feedback だけを hydrate するようにした。latest / best / next step / status card の意味は維持し、DB / API / auth / storage policy は変えていない。
- Review data loading consolidation として、`/scripts/[id]/review/[takeId]` は current take、best/latest comparison、status card 用 context を対象 script の hydrated reviews 1回から組み立てるようにした。canonical source、score、best take 判定、DB / API / auth / storage policy は変えていない。
- Protected take audio replay は、保存済み録音の client fetch / blob URL 化が終わるまで native controls を出さない前提を維持しつつ、準備中 / 準備完了 / 失敗 / 再読み込みの feedback と development-only client timing を追加した。take audio route、ownership check、storage policy、API contract は変えていない。
- Progress の保存済み録音 / 保存済みお手本は、初期表示では見やすい `聞く` ボタンだけを出し、押した音声だけ `ProtectedAudioPlayer` を mount して protected fetch / blob URL 化する。Review first view の録音再生は即時確認を優先し、lazy load にはしていない。auth / ownership / storage policy / replay route は変えていない。
- Record の evaluate 待ち UX は、client 側の時間経過メッセージで `Take を送っています` -> `声を文字にしています` -> `発音の目安を見ています` -> `次の1点をまとめています` -> `Take メモを保存しています` を出す。これは正確な進捗率ではなく待ち体験の feedback で、`/api/evaluate` response shape、評価処理、review persistence、DB / provider contract は変えていない。
- Gate1b の protected replay / cross-user ownership proof は human browser で PASS。User A の script-audio / take-audio replay は 200 相当で再生でき、User B では同じ音声が 403/404 相当で再生できなかった。raw URL / raw id / signed URL / user id / storage path は記録していない。
- main loop の retry UX と、次の一手の分かりやすさを small diff で上げ続ける。
- voice provider 本接続の準備を、小さい差分で進める。
- OpenAI voice の entitlement 待ちに引きずられず、v1 の voice provider 本線は ElevenLabs IVC で進める。
- setup/voice / listen / preflight で、mock 本線・provider 固定前提・entitlement 依存を混同しないように保つ。
- 広い refactor は避ける。
- E2E 拡張は、現行 smoke path を壊す変更が出たときだけ優先する。
- Playwright の webServer は `PLAYWRIGHT_PORT` ごとの `.next-e2e-*` dist dir を使う。手元の `next dev` や別ポートの test run と build output が競合しにくい。
- `npm run dev` は `.next-dev` を使う。`npm run build` が production 用 `.next` を作り直しても、起動中 dev server の CSS chunk を消さないため。
- Home の CSS / root route 確認は、起動中の `http://localhost:3000/` に対して `npm run home:css-smoke` を使う。これは Playwright webServer の別ポートではなく、人間が見る dev server を直接確認する。

## 手動確認の目安
1. `login` する。
2. `/setup/voice` で同意と必要な録音 upload を含む voice 設定を整える。
3. `/scripts` で script を作るか複製する。
4. `/scripts/[id]/listen` で見本確認をする。
5. 声や評価の準備が不足しているときは、その場の案内から `voice 設定` や `scripts` に戻る。
6. `/scripts/[id]/record` で録音を保存する。Azure のときは非 wav 録音も client 側で wav / PCM に寄せてから保存する。
7. `/scripts/[id]/review/[takeId]` で保存済み結果を確認する。
8. `/progress` で5本までの練習スロットから成果を選んで見る。
