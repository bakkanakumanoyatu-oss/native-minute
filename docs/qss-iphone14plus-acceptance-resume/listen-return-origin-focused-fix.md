# Listen return-origin focused fix — 2026-09-18

**MODE: QSS_REVIEW_LISTEN_RETURN_ORIGIN_CLOSEOUT — HUMAN ACTUAL-DEVICE PASS / CLOSED.**

Human confirmed on the same updated Staging iPhone 14 Plus: **Review → お手本を聞き直す → Listen → 戻る = 元のReview / same Take**. This closes the Navigation finding. PS-N-01 is PASS with the updated return-origin contract; RV-06 (same-script Review → Listen) is also PASS. Existing Review→Record→Back, Progress→Record→Back, normal Listen→Record→Back, unsaved-exit and Favorite PASS remain valid. P1/P2/P3/J/Gate5 and Listen playback remain CLOSED; upload/evaluation success evidence remains untouched.

Closeout scope: `PracticeApp.tsx`, `routes.ts`, `routes.test.ts`, scoped mapping/acceptance entries and selected verification evidence. The accepted Record return-origin prerequisite is included because Listen reuses its helper/signature and all its relevant routes are Human PASS. Progress screen/styles/tests and unrelated acceptance/evaluation WIP are excluded. Shared documentation is staged by content so unrelated earlier changes remain unstaged; committed counts describe only committed entries, while working-copy counts also include preserved acceptance WIP.

Validation reused: 122 tests, 50 Chromium/WebKit navigation conditions, root/mobile lint and typecheck, root/Staging/native builds, focused self-review and 13:37 update-install receipt/identity. Source hashes still match the tested/installed mobile source. No tests, lint, typecheck, build or install repeated during closeout. Workspace guard, mapping/count consistency and staged diff checks only. Archived log whitespace is normalized for git diff checks; test results are unchanged. Installed artifact retains its honest pre-commit sourceRevision/sourceDirty metadata; no release guard rewrite.

Commit/push: authorized by this Human PASS; one normal commit/push of the scoped selection. Delivery SHA, upstream/remote and residual WIP are reported in the closeout response. No Home implementation, copy change, Progress visual work, slot decision, Dynamic Type/VoiceOver, template, performance, DB/BFF/provider/deploy or Gate5 work.

**NEXT_ONE_ACTION:** 次の独立WIPとして「HomeからReviewを経由せず練習を始められる導線」へ進む。

The following implementation/install notes are historical (before Human PASS):

1. **MODE / preflight:** `QSS_REVIEW_LISTEN_RETURN_ORIGIN_FOCUSED_FIX`. Developer checkout / workspace guard PASS; branch `codex/g3-mobile-main-loop`, HEAD `24a647bf9283d3a82f4011986b9e783c3871a13a`. Existing uncommitted Navigation/Progress/acceptance work retained, index empty. Only the new Listen return-origin finding is in scope; P1/P2/P3/J/Gate5 remain CLOSED.
2. **Root cause:** Record captured its immediate internal origin; Listen used the broad practice origin, which was Home in the reported flow. Review's existing Listen action already identifies the same script, but its exact Take was not retained by the shell.
3. **Fix:** Capture the current Review route (scriptId + takeId) on internal navigation to Listen, using the same in-memory ref / route helper approach as Record. Accept only a same-script Review with a safe Take segment. Preserve through same-script Listen → Record → Back; clear on unrelated navigation, reload and popstate. Invalid/mismatched origin falls back to the existing broad safe origin. Query parameters are ignored. Existing server-owned Review fetching, ownership and unavailable-data handling remain intact; no new server existence lookup. Leave guard runs before context mutation; Home exit is unchanged. No history.back dependency or new navigation framework.
4. **Changed files this task:** `apps/mobile/src/practice/PracticeApp.tsx`, `routes.ts`, `routes.test.ts`; this report and a short `docs/current-state.md` entry. Evidence under `outputs/qss-listen-return-fix/`. Existing Home/Progress/styles/Favorite/script-limit/DB/schema/BFF/provider files have no incremental changes.
5. **Tests / validation:** Workspace guard, root/mobile lint, root/mobile typecheck, root Next build, Staging build/sync, signed native build and diff check PASS. 122 scoped tests PASS (route/helper, shell timing, record state/protection, Listen playback regression). 50 Chromium/WebKit navigation conditions PASS, including best and latest exact Take returns, Review → Listen → Record → Back → Listen → Back → Review, normal Home/Scripts entry, existing Record returns, unsaved Cancel/discard, Home exit, URL/reload/popstate fallback. One focused self-review of only this Navigation diff: PASS / 0 must-fix. Initial QA selectors were corrected to existing accessible names; no product copy change. Full58 / full E2E / live evaluation or provider / physical VoiceOver and Dynamic Type were not rerun. Automated PASS is not Human PASS.
6. **Build/install:** 2026-09-18 13:37 JST Staging `com.nativeminutes.app.staging`, version 1.0 (1), successfully update-installed on the same iPhone 14 Plus. Artifact `/tmp/qss-listen-return-fix/DerivedData/Build/Products/Staging-iphoneos/App.app`. Strict signature and provisioning PASS; all dist/synced/signed web asset hashes match. Receipt URL matches device inventory (one Staging app). No data clear, uninstall or automatic launch. No private app-data audit. Source is HEAD + existing WIP + this focused diff. Release guard remains NOT PASS only for the known three dirty-source categories (dist/native metadata, source tree); no guard or metadata bypass, no temporary commit. User explicitly authorized pre-Human WIP install.
7. **Human one operation only:** On the updated Staging app, open a saved Review, then **お手本を聞き直す → Listen → 戻る**. Expected: original Review / same Take. Reply PASS or the actual destination. Do not repeat any existing PASS checks.
8. **Files/status:** LOCAL PASS / STAGING INSTALLED / HUMAN PENDING. No stage/commit/push, backend deploy, migration, provider call or Production operation. Earlier evidence and closed gates retained. Product scope verified against preflight hashes, not the whole pre-existing working-tree diff.
9. **NEXT_ONE_ACTION:** Human performs only the operation in item 7. Wait for Human PASS before commit/push.

Evidence: [focused diff](../../outputs/qss-listen-return-fix/focused-source-diff.json), [tests](../../outputs/qss-listen-return-fix/tests.log), [browser results](../../outputs/qss-listen-return-fix/browser-results.json), [focused review](../../outputs/qss-listen-return-fix/focused-review.json), [validation](../../outputs/qss-listen-return-fix/validation.json), [artifact](../../outputs/qss-listen-return-fix/native-artifact-proof.json), [install](../../outputs/qss-listen-return-fix/install-status.json).
