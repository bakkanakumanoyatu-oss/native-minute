# Home Favorite → Review → Back focused fix — 2026-09-18

**MODE: QSS_HOME_FAVORITE_REVIEW_RETURN_CLOSEOUT — HUMAN ACTUAL-DEVICE PASS / CLOSED.**

Human confirmed on the same updated Staging iPhone 14 Plus: **Home → お気に入りの録音 → Review → 戻る = Home**. PS-N-01 retains PASS with this new evidence. P1/P2/P3/J/Gate5, Review→Listen→Back and Listen playback remain CLOSED. Review→Record→Back, Progress→Record→Back, Record/evaluate success, Favorite/Share and unsaved-exit PASS evidence is preserved. No additional Human observations are inferred.

Closeout scope: only the reviewed Review return-origin changes in `PracticeApp.tsx`, `routes.ts`, `routes.test.ts`, the scoped mapping/acceptance additions and selected verification evidence. Shared documentation is staged by content; unrelated working-copy acceptance entries/counts remain unstaged. Home-entry copy/CTA changes, Home visual/selection work and Progress implementation are excluded. This closeout adds no product source edits.

Validation reused: 71 scoped tests, 100 Chromium/WebKit navigation conditions, root/mobile lint/typecheck, root/Staging/native builds, focused review and 14:21 update-install artifact/receipt proof. All current mobile source hashes still match the tested/installed source. No tests, lint, typecheck, builds or installs rerun. Only workspace, source identity, mapping/count consistency and staged-diff checks are performed. Installed metadata retains its pre-commit sourceRevision/sourceDirty values and known release-guard findings; no metadata rewrite.

Human PASS explicitly authorizes one scoped commit and normal push. Delivery SHA, upstream/remote, ahead-behind and residual WIP are reported in the closeout response. No other WIP implementation, backend deploy, provider/DB operation or new acceptance session is started.

**NEXT_ONE_ACTION:** 次の独立WIPとして「Homeの上段台本の選定理由・最近の練習CTA・情報密度」を扱う。

The following implementation/install notes are historical (before Human PASS):

1. **MODE / preflight:** `QSS_HOME_FAVORITE_REVIEW_RETURN_ORIGIN_FOCUSED_FIX`. Correct Developer checkout; workspace guard PASS. Branch `codex/g3-mobile-main-loop`; HEAD/upstream/remote `f39977c3ca56ef066b620bd02fe3b73964969b54`. Existing Home-entry, Progress, acceptance and output WIP preserved; index empty. P1/P2/P3/J/Gate5, Listen playback and accepted Listen/Record return-origin remain CLOSED. Earlier Home-entry Human acceptance is not inferred from this finding.
2. **Root cause:** Home Favorite uses the existing saved Take row action to open Review with the correct scriptId/takeId. `practiceBackRoute` unconditionally returned same-script Record for every Review, ignoring the actual entry. The same fallback also applied to My Takes and Progress saved Review entries; their UI callbacks already identify the correct saved Take.
3. **Fix:** Reuse the shell's internal in-memory ref + Back-helper approach. Capture explicit Home / My Takes / Progress entry with the exact Review script and Take. Review Back returns only a matching safe entry, preserving list filters/script context; unknown entry, newly evaluated Take, wrong script/Take or invalid destination retains Record fallback. Same-script Listen/Record detours preserve the saved Review entry. Non-practice navigation, another script/Take, reload and popstate clear it. URL origin parameters remain unused. Leave guard runs before mutations. Returning to My Takes preserves its own parent so the next Back cannot loop into Review. No new navigation framework, storage or API.
4. **Changed files:** `apps/mobile/src/practice/PracticeApp.tsx`, `routes.ts`, `routes.test.ts`; this report, a short `docs/current-state.md` entry and `outputs/qss-home-review-return-fix/` evidence. Home selection/copy/CTA/layout/CSS, Progress, script limits, Favorite Script/templates, DB/BFF/provider and performance have no incremental changes.
5. **Tests / validation:** 71 scoped Home/route/shell tests PASS. 100 Chromium/WebKit navigation conditions PASS: Home Favorite → Review → Home; My Takes → Review → exact My Takes (including favorite/script filters); Progress → Review → exact Progress; Review→Listen/Record→Back same Take; nested Listen→Record detour then back through Review to entry; existing Progress/Listen→Record returns; My Takes parent without a loop; Home exit; unsaved Cancel/discard; reload/popstate/direct URL fallbacks. Newly evaluated Take rejection is unit-tested; browser fallback after a new Review URL uses a synthetic popstate, not a live evaluation. Root/mobile lint/typecheck, root build and Staging build/sync PASS. Navigation-only self-review PASS / 0 must-fix. Full E2E / 58-item acceptance / live evaluation or provider / physical VoiceOver and Dynamic Type not rerun. The unrelated pre-existing Progress English-label assertion failure recorded in the prior Home task was not rerun or changed.
6. **Build/install:** 2026-09-18 14:21 JST same iPhone 14 Plus successfully update-installed with Staging `com.nativeminutes.app.staging` 1.0 (1). Artifact `/tmp/qss-home-review-return-fix/DerivedData/Build/Products/Staging-iphoneos/App.app`. Strict signature, provisioning for this device, Staging public auth fingerprint/domain, dist → synced → signed web asset hashes PASS; exact install receipt matches device inventory (one Staging app). Build source hashes unchanged through install. No data clear, uninstall or automatic launch; no private app-data audit or device-bundle extraction. Release guard remains NOT PASS for the known three dirty-source categories (dist/native metadata and source tree); user explicitly authorized WIP install before Human PASS. No guard/metadata bypass or temporary commit.
7. **Human one operation:** On the updated Staging app, **Home → お気に入りの録音 → Review → 戻る = Home**. Reply PASS or the actual destination. No repeat of other CLOSED checks.
8. **Files/status:** LOCAL NAVIGATION PASS / STAGING INSTALLED / HUMAN PENDING. No stage/commit/push, backend deploy, migration, provider call or Production operation. Existing Home/Progress/acceptance WIP retained. Diff check PASS; only the three scoped implementation/test files and current-state note changed among pre-existing tracked files.
9. **NEXT_ONE_ACTION:** Human performs only item 7 on the updated Staging app. Wait for Human PASS before commit/push.

Evidence: [focused diff](../../outputs/qss-home-review-return-fix/focused-source-diff.patch), [review](../../outputs/qss-home-review-return-fix/focused-review.json), [tests](../../outputs/qss-home-review-return-fix/tests.log), [browser](../../outputs/qss-home-review-return-fix/browser-results.json), [validation](../../outputs/qss-home-review-return-fix/validation.json), [artifact](../../outputs/qss-home-review-return-fix/native-artifact-proof.json), [install](../../outputs/qss-home-review-return-fix/install-status.json).
