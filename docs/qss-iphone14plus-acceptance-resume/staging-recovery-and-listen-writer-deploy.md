# Staging recovery and Listen writer deployment — 2026-09-17

MODE: `QSS_POST_PROVIDER_FAILURE_STAGING_RECOVERY_AND_LISTEN_WRITER_DEPLOY`

**STAGING RECOVERED / WRITER DEPLOYED / HUMAN DEVICE RETRY PENDING.** P1/P2/P3/J/Gate5 remain CLOSED; formal acceptance remains PAUSED. No device result is claimed.

- Preflight: Developer checkout, `codex/g3-mobile-main-loop`; HEAD/upstream/remote all `910bfa46f63783966c50242b3ac6067dd4ce3f6b`. Existing WIP preserved. Protected secret backup / `.env.local.save` / `supabase/.temp` contents were not read, hashed or changed.
- Fresh exact target: `42d4da86-1f25-4f91-be18-fbb2b94f2844`. Original 18:13 completed Storage-failure event, owner/script/default voice, canonical cache calculation, event cache hash, persisted locator and expected version matched. Reserved, lease expired, canonical row absent; no active deletion prerequisite conflict. Physical absence was not required or asserted.
- Only 0032 was applied, atomically with its migration ledger entry. Ledger 0001–0032 and exact 0032 body verified. New status/constraints, three trigger functions, three triggers and service-role-only recovery EXECUTE boundary verified. Existing reserve/finalize/cancel functions and owner unresolved index unchanged. No historical migration replay or drift repair.
- Exactly one formal RPC invocation returned HTTP 200: `reserved -> failed_after_provider`, recovered at `2026-09-17T10:14:09.364068Z`. Identity, original evidence and locator retained; lease cleared; `provider_effect=occurred`, `storage_outcome=failed`, `orphan_possible=true`. Evidence reference `d96e7fde-587d-4cf0-8e5b-cb0902197b55` has a restricted durable incident record outside Git, without credentials, lease token, audio or private URLs.
- Post-recovery and post-deploy: owner unresolved intents **0**, canonical rows for the exact tuple/locator **0**, other **39** intents unchanged. The retired ID is no longer reserved; unchanged finalizer/cancel preconditions and existing isolated tests establish non-reuse. No live finalize/cancel/reserve probe was issued. The separate orphan cleanup/deletion obligation remains.
- Writer patch is byte-for-byte the existing `26a8b3d` two-file diff. Revalidated all 658 baseline sources; only `services/voice/voice.service.ts` and `services/voice/replay.service.ts` differ. No root product code, UI, native input, environment configuration or patch content changed.
- New deployment **`dpl_GFTxhyxGxhCf34wQwMmAQ58TiKhd`**, project **`prj_RSxUzxugEIlH28tiQOyndjP365hw` / `native-minute-staging`**, is READY. Both existing Staging aliases and the project target point to it. Vercel calls this isolated Staging project's existing public target `production`; the actual Production project/database were not accessed. Staging environment metadata is unchanged.
- All 658 deployed source hashes match the candidate. Source manifest SHA-256: `1d14ee8e86b5464a861dde5ca232905173877b8427296b8eaf7b11d69a439025`. Patch SHA-256: `db98ab731e7c8b0b3b4e86a2b7a0ec9710ceacb9ecfcdad1d1a759fd7b729a56`. Server-owned Storage writer injection and exact-byte duplicate reconciliation confirmed. Old deployment is no longer the active Staging alias target.
- Generation-event count remains **13**, latest still the original 18:13 attempt. This task issued **0** application/Listen requests, provider calls and Storage deletes. No synthetic TTS or post-deploy runtime playback test.

Validation: workspace check, 8 scoped suites / **81 tests**, candidate 2 suites / **5 tests**, root lint/typecheck, candidate BFF build and post-build typecheck, diff check **PASS**. Root build, native build/install, device retry, expanded E2E, Gate5/full58/A–K retests **NOT RUN**. No new root product implementation required a root build. Prior isolated PostgreSQL/race proof was not rerun; it remains the separately recorded local result.

[Safe result / deployment hashes](../../outputs/qss-listen-voice-readiness/staging-recovery-result.json). Stage/commit/push **0/0/0**. Current task adds this report/result and a current-state pointer; earlier migration/types/tests/docs WIP remains uncommitted.

NEXT_ONE_ACTION: Human uses the same iPhone 14 Plus / Staging / script: open Listen, press prepare exactly once, hear speaker playback, pause, resume, observe advancing media time, leave Listen, re-enter and reuse the same reference. No voice registration, setup recreation, reinstall, migration or Share/Files retest. STOP for that device result. After Human retry, a separate read-only server check must establish a new completed intent, canonical script_audio, cache reuse and no additional provider generation for re-entry.
