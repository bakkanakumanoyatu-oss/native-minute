# G5C-B1 Durable State / Repository Foundation

Status: `CLOSED_COMMITTED_PASS`

Scope is limited to migration `0015_g5c_b1_voice_deletion_durable_state.sql`, aligned TypeScript DB types, and a server-only repository for voice-only deletion operations and targets.

`0015_g5c_b1_voice_deletion_durable_state.sql` is `STAGING_APPLIED_VERIFIED_PASS` on canonical Staging `native-minute-staging` (`ztlliqishddrrvqqrrlu`, `ap-northeast-1`, `ACTIVE_HEALTHY`). Pre-apply migration history was exactly `0001`–`0014` applied with `0015` as the only pending migration; post-apply history is exactly `0001`–`0015` applied.

The `create_or_get_voice_deletion_operation` `RETURNS TABLE` contract remains unchanged. Its generated Database type models the PostgREST result as an array, while the repository explicitly applies `.single()` and fails closed if the RPC does not yield exactly one row. The actual Staging service-role `.single()` path returned exactly one valid row for creation and reuse.

- Actual catalog proof: both durable tables exist with RLS enabled. `PUBLIC`, `anon`, and `authenticated` have no table privileges; `service_role` has `SELECT` only and no `INSERT`, `UPDATE`, `DELETE`, `TRUNCATE`, `REFERENCES`, or `TRIGGER`. All five mutation RPCs have fixed expected signatures, owner `postgres`, `SECURITY DEFINER`, `search_path=pg_catalog, public`, no execute for `PUBLIC`/`anon`/`authenticated`, and execute only for `service_role`.
- Disposable authenticated-fixture proof: direct `SELECT`/`INSERT`/`UPDATE`/`DELETE` on both tables and direct execution of all five mutation RPCs were rejected. Server-side reads confirmed no direct-operation state change.
- Service-role proof: direct table `SELECT` was allowed; direct operation and target `INSERT`/`UPDATE`/`DELETE`, including sealed-target insert/delete/reparent/locator tampering, were rejected. Mutation is therefore restricted to the focused RPCs in actual Staging.
- Durable-flow proof: create-or-get returned `created=true` then the same ID with `created=false`, with one active operation; a synthetic provider target sealed successfully; a duplicate target-fingerprint seal rolled back with zero partial targets and no succeeded snapshot; reseal was rejected; lease claim passed; a competing claim did not acquire; wrong-token release did not mutate; correct release and re-claim passed; unsafe finalization was rejected and did not complete the operation. Staging returned zero-row lease RPCs as all-null composites rather than JSON null, so the repository now validates the returned operation ID and maps them to `null` / `false`; an actual repository Staging test passed for competing claim, wrong-token release, and correct release.
- Actual trigger and constraint catalog proof confirmed operation-transition and target-immutability triggers, completed-state safety, failed-before-destructive safety, and target-kind-specific verified-absence constraints.
- The disposable fixture user was deleted through the server-only Admin path. Auth absence and cascade removal of its operations and targets were confirmed. Existing users and data were not touched. Provider calls, Storage mutations, and consent mutations were all `0`.

P0=`0`; P1=`0`. Accepted non-blocking P2 follow-up is limited to actual concurrent create race, actual expired-lease reclaim, and actual wrong-plane/destructive-monotonic transition proof once those future transition RPCs exist. Current unimplemented scope remains provider deletion/reconciliation, Storage cleanup, consent-withdrawal runtime, target-transition runner, live voice-only destructive proof, retention purge, account-deletion integration, and learning-history targets.
